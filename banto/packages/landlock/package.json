{
  "name": "@banto/landlock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build:rust": "cd rust-launcher && cargo build --release",
    "build": "npm run build:rust && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}
