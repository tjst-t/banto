# LLM・モデル管理 実装メモ

ADR-0004（使えるLLMは全体設定、モデルは動的選定）と ADR-0011（LLM管理は中核が持つ）の実装まとめ。

## 何を決める仕組みか

番頭と職人が使うモデルを全体設定で持つ。プロジェクト個別には持たない。

- **番頭は具体モデル**を持つ。連続した1つの会話なので、途中で勝手に変わってほしくない
- **職人は tier で指定**する。タスクごとに起こすので、そのときの難度に合ったモデルを registry が解決する

番頭が職人へ振るときに出すのは**モデル名ではなく `(tier, 制約)`**。こうすると、モデルが入れ替わっても番頭の判断がそのまま使え、「なぜそれが選ばれたか」を後から追える。

## tier と制約は直交する

| | 役割 | 例 |
|---|---|---|
| **tier** | 難度の軸。3段階 | 高精度 / 通常 / 高速 |
| **制約** | 候補を絞る条件 | 画像を読む / 外に出さない / 有料キーを使わない |

**制約は決して緩めない。** 「外に出さない」と言われて満たせるモデルが無いとき、外へ出るモデルを返してはならない——解決せずに返す（I2）。緩めてよいのは tier だけで、落ちたときは `usedFallbackTier` で呼び出し側に伝える。

tier ごとに説明文（番頭が「このタスクはどこか」を決める基準）と第一候補を持つ。第一候補が制約で落ちたら、同じ tier の次の候補に降りる（`droppedPick`）。

## 出どころは2つに分かれる

| | 置き場所 |
|---|---|
| どのモデル・どの鍵が**存在するか** | pi の `~/.pi/agent/models.json` / `auth.json` |
| tier・既定・使用可・キーの並び順 | banto の `llm-registry.json` |

pi は**設定ファイル経由でのみ**扱い、pi 本体のコードには手を入れない。

pi の設定ファイルは人が直接編集することもあるので、読み込み時のハッシュを保持して外部変更を検知する。書き込み直前にも同じ照合をすれば、監視が取りこぼしても人の編集を上書きしない。

## キーは上から順に消費する

同じプロバイダに複数の鍵を持ち、上限に来たら次へ落ちる、という運用のための仕組み。

- **並び順と役割の許可は設定**（`llm-registry.json`）
- **キーが上限かどうかは実行時状態**でファイルに残さない（D3：ファイルは意図、実行時状態は別）。待ち時間が過ぎたキーは自動で候補に戻る

役割（番頭 / 職人）は順序ではなくフィルタとして持つ。個人鍵は職人だけ、仕事鍵は番頭だけ、といった分け方ができる。並びを2本持たずに済む。

> 現状 `auth.json` は「プロバイダ名 → 鍵」の形なので、1プロバイダ1鍵。複数鍵を持てるようにするのは `auth.json` のデータモデル変更で、鍵の保存方式（sops / OSキーチェーン / 暗号化ファイル）の ADR 待ち（D1）。並び順と役割の仕組みは先に入れてあるので、鍵が複数になればそのまま効く。

## どこに実装があるか

| ファイル | 中身 |
|---|---|
| `packages/banto-core/src/llm-registry.ts` | `LlmCatalog`。解決・オーバーレイ・キー・外部変更検知 |
| `packages/banto-host/src/llm-tools.ts` | `llm.*`（中核ドメイン。モジュールではない） |
| `packages/banto-host/src/core-settings.ts` | 設定画面の LLM の区画 |
| `packages/banto-host/src/module-serve.ts` | `/api/core/tools/{名前}`（中核の Tool 面） |
| `packages/banto-web/src/views/LlmRegistryViewer.tsx` | 設定画面に出す GUI |
| `packages/banto-worker-pool/src/pi-rpc-driver.ts` | spawn 時に `(tier, 制約)` から解決 |
| `tests/acceptance/llm-registry.spec.ts` | 受け入れテスト17件 |

### モジュールではなく中核（ADR-0011 決定42）

`bin.ts` はモジュールレジストリより前に `LlmCatalog` を組み、`resolveHostDefault()` で番頭のセッションを作る。**中核がカタログに依存していて、モジュールが中核に依存している**——モジュールの中身が先に必要になる時点で、それはモジュールではない。

`canvas.*` / `memory.*` / `skill.*` と同格の中核ドメインとして扱う。基準は「それが無いと番頭が番頭でなくなるか」。番頭は LLM セッションそのものなので、モデルは獲得する能力ではなく乗っている土台にあたる。

### GUI は設定画面だけ

項目の宣言（`SettingField[]`）では表せないので、中核の区画だけが `view` で描き先を宣言できる（決定43）。モジュールには決定41（GUI は渡さない）がそのまま効く。キャンバスには出さない——設定であって会話の材料ではない。

## Tool

| Tool | 用途 |
|---|---|
| `llm.list` | プロバイダ・モデル・キー・tier・既定の一覧 |
| `llm.resolve` | `(tier, 制約)` → 具体モデルとキー |
| `llm.set_tier` / `llm.set_tier_description` | モデルの tier / tier の基準 |
| `llm.set_host_default` / `llm.set_worker_tier` | 番頭の既定モデル / 職人の既定 tier |
| `llm.set_pick` / `llm.set_usable` | tier の第一候補 / 役割ごとの使用可 |
| `llm.set_provider_local` | 外に出ないプロバイダの指定（制約 local の候補） |
| `llm.set_key_order` / `llm.set_key_scope` | キーの並び順 / 役割の許可 |
| `llm.reload` | pi の設定ファイルを読み直す |

```ts
llm.resolve({ tier: "reasoning", constraints: { local: true } })
```

## 解決先を失う操作は止める

- 番頭の既定モデルは番頭の使用可から外せない
- tier の第一候補は職人の使用可から外せない／そのまま tier を移せない
- プロバイダで最後の1本になるキーから、その役割の許可を外せない

いずれも黙って通さず、何を先にすべきかを添えて断る（I2）。

## 移行

起動時に旧形式から自動で移る。

- `settings.json` の `llm.provider` / `llm.model` → `defaults.host`
- `modules["worker-pool"].provider` / `.model` → 既定 tier ＋ その tier の第一候補
- `llm-registry.json` の `defaults.worker`（具体モデル） → 同上

「そのモデルを職人の既定にしていた」は「その tier が既定で、その tier の第一候補がそのモデル」と読み替えられる。意味は変わらない。

## 未実装

- **キーの追加・削除**（`auth.json` への書き込み）。鍵の保存方式が ADR 待ち（D1）
- **カスタムモデルの追加**（`models.json` への書き込み）
- 書き込み直前のハッシュ照合。検知の仕組みは入っているが、書き込む操作がまだ無いので当てる先が無い
