---
id: verification-log-r6-wiring
type: note
status: draft
refs: [spec-memory, adr-0010, memory-verification-scenarios]
---

# R6 配線チェック（§8.5 検索機構の新設後）

**対象**: docs/spec/memory.md / docs/adr/adr-0010-memory-substrate-schema.md
**検査者**: 配線チェック専任エージェント
**検査項目**: 検証プロトコル §2.4 の1〜7

## 指摘一覧

| # | 箇所 | 種別(1-7) | 該当節 | 内容 | 修正案 |
|---|---|---|---|---|---|
| W1 | memory.md:1471 | 1 | §20.7.1 / §20.5 | レジストリの行名が「**自己内省 6項目**」だが、§20.5 の点検表は**7項目**（7＝索引と本体の乖離を同日に追加している）。行名が本文と一致しない | 「自己内省 7項目」に直す。または件数を行名に書かない（項目が増えるたびにズレる） |
| W2 | memory.md:1471 | 1 | §20.7.1 / §20.5 | 同行の **優先度・通知・提示面の3欄が「項目ごと」** と書かれているが、§20.5 の表の列は `# / 点検 / 見つけたら何をするか / 結果の行き先` の4列だけで、**優先度欄も通知欄も提示面欄も存在しない**。「項目ごと（同表）」の参照先が無い＝表に書いてあるだけで本文に無い（検査項目1そのもの） | §20.5 の表に「優先度」「通知」「提示面」の3列を足す。§20.7.1 が「欄を埋めるときは本文に裏付けの節があることを確認する」と自ら定めた規律の適用対象 |
| W3 | memory.md:1480 vs 711 | 1・7 | §20.7.1 / §8.5.4 | レジストリの横断ファンアウト行は発火条件の3つ目を「**PO 主体のセッション**」と書くが、§8.5.4 の3つ目の AND は「**`mode == context \| recall`**」。両者は同値ではない（自律ループのセッションも `mode == context` で走るので、本文では発火し、レジストリでは発火しない）。かつ「PO 主体のセッション」という語は本文のどこにも定義が無い（§7-5 は「`po_message` 由来のセッション」という別の語を使う） | どちらかに寄せる。抑制したいのが自律ループなら §8.5.4 の条件3を `mode == context \| recall` **かつ** `po_message` 由来のセッション、に直し、レジストリを同文にする |
| W4 | memory.md:1476, 1484, 1497 | 1 | §20.7.1 | 提示面欄の値が値域を超えている。(a) 候補生成パイプライン行の「trace は **Memory 面**」— Memory 面の定義は「記憶の一覧・絞り込み・訂正導線」であり trace 表示を含まない。さらに §16 は「trace はセッション側に持ちエントリには書かない」と定めるので、**セッション終了後に Memory 面から参照できる保証が無い**。(b) 索引の健全性点検行の「Memory 面」も同様に値域定義の外。(c) 値域3つのうち **アテンションキューを使う行が1つも無い**（§20.5-6 は「アテンションカードを作る」と書くのに、自己内省行の提示面は「項目ごと」で空回り） | Memory 面の定義に「検索 trace の閲覧」「索引健全性」を含めるか、trace の保持期間を §16 に書く。§20.5-6 の提示面を「アテンションキュー」として明示する |
| W5 | memory.md:1479 vs 1315-1322, 1340-1351 | 1・3 | §20.7.1 / §20.2 / §20.3 | 索引の再構築行の発火3つのうち「**アーム構成の変更**」に対応する起床源が §20.2 に無い（起床源は6種で、そこに該当なし）。また「再索引ジョブを積む」（§20.5-7・§8.5.6）は**キューに乗る作業種別**なのに §20.3 の優先度表に行が無い。§20.3 は「この表に行が無い起床源・作業種別を作らない」と明記しており違反 | §20.2 に `config_change`（アーム構成・埋め込みモデルの変更）を足すか、`model_migration` に含めることを明記。§20.3 に「再索引ジョブ（低）」の行を足す |
| W6 | memory.md:1463-1487 | 2 | §20.7.1 / §0.3.0 / §0.3.1 | **不変条件 I1〜I7 と §0.3.1 の書き込み前バリデーション（`citations` 空振り検出）が、レジストリに1行も無い。** 登録されているのは I8（索引の更新）だけ。R5 が「防ごうとした欠陥を一段上で再現していた」と指摘したのと同型で、**書き込み経路の強制機構そのものが未登録＝配線が保証されていない** | 「書き込み経路の不変条件（§0.3.0 I1〜I7）」と「`record()` 書き込み前バリデーション（§0.3.1）」の2行を足す。発火＝§0.3.0 の経路一覧、④強制＝実行層、⑥＝拒否（I6） |
| W7 | memory.md:1463-1487 | 2 | §20.7.1 / §8 / §7 | **話題転換の検出（§8）が未登録。** これは §7 のバッチ退場と §7-5 の「`sensitive` を即時バッチ退場させる」の唯一の発火条件であり、機構が無ければ機微記憶の滞在ゲートが効かない。判定主体・閾値・入力も未定義（§0.9-6 に「話題転換検出の閾値」があるのは値の話で、機構の登録とは別） | 「話題転換の検出（§8）」を1行足す。①発火＝毎ターン、⑤入力＝発話とワーキングセットの類似度、②＝マイクロスリープ発火＋`sensitive` の強制退場、⑥＝ワーキングセット |
| W8 | memory.md:1463-1487 | 2 | §20.7.1 / §18 / §20.7 | **睡眠の発火（`fatigue` のソフト／ハード閾値）が未登録。** 深い睡眠はレジストリの8行が「①発火」として依存している最重要のトリガーなのに、それ自身に行が無い | 「睡眠の発火（§18・§20.7）」を1行足す |
| W9 | memory.md:1466 | 2 | §20.7.1 / §14 | 同一バッチ内の対立検査行が ⑤入力を「**`propose()`（§14 の呼び出し条件）**」としているが、**その `propose()` を呼ぶ機構（浅い睡眠がセッション終了時に §14 の5条件で呼ぶ）自身に行が無い。** 入力の供給元が未登録なので、上流が動くかを表から検査できない | 「候補キューへの投入（§14 `propose()` の呼び出し条件）」を1行足す |
| W10 | memory.md:508-514 | 2・4 | §7 休眠トピックの復帰 / §8.5 | **休眠救済の免除規則（強いエンティティ一致があれば文脈一致度・目標関連性・確信度の経過時間ペナルティの減点を免除する）が、レジストリに未登録であり、かつ §8.5.0 のどの Stage にも現れない。** §8.5.0 の Stage 3 は「RRF で融合し `rel_norm` を作る → `s_admit` を算出」としか書かず、免除の適用点が無い。§7 は「発火経路は A2」と言うが、**A2 のヒットを読んで減点を免除する処理がスコア式にも段にも落ちていない**（＝§20.0 と §6.4 が最重要と記録する「散文だけで実行層に落ちていない」パターンの現物） | Stage 3 に「A2 ヒット時の免除」を明示的な処理として書き、`s_admit` の式に免除の効き方（例：w2・w3・w4 の該当成分をフロア値に置換）を書く。レジストリにも1行足す |
| W11 | memory.md:174 | 2・4 | §20.7.1 / §0.3.1 | `assemble_brief()` の「**直近ターンで `sensitive` エントリがワーキングセットに滞在していた場合、`task` の生成を権限を縮小した別呼び出しに切り出す**」が未登録。発火条件の入力（直近ターンの滞在履歴）を誰が保持し誰が判定するかも未定義 | レジストリに1行。⑤入力＝セッション側の滞在状態テーブル（§2）、④強制＝ハーネス |
| W12 | memory.md:189-192 | 2 | §20.7.1 / §0.3.1 | 「**`derived_from` の自動付記**（ハーネスが `build_context()`／`assemble_brief()` の載せた記憶IDを機械的に渡す）」が未登録。`retrieve()` の `ranked[]` からどう `included_refs[]`／`derived_from` 候補になるかの対応も §8.5 側に書かれていない | レジストリに1行。加えて §8.5.0 に「`ranked[]` のうち注入されたものが `included_refs[]` になる」と書く |
| W13 | memory.md:643, 825 | 2・4 | §20.7.1 / §8.5.1 | **エイリアス表の更新経路が未登録・API 無し。** §8.5.1 は供給元表に「エイリアス表（PO が訂正でき、`user_explicit` が `agent_inferred` に勝つ）」と書き、§8.5.8 も「`user_explicit` が勝つ」を対策に挙げるが、**エイリアス表を書く操作が §0.3 に無く**（`supersede()` はエントリ用）、優先規則をどこで強制するかも未定義。「いつ入るか」欄も timing になっていない | エイリアス表を閉包レジストリと同じ Policy データとして扱い、書き込み API と origin 優先の強制箇所を §8.5.1 に書く。レジストリに1行 |
| W14 | memory.md:638-643 vs 121 | 2・6 | §8.5.1 / §0.3.0 I8 | `entity_index` の供給元5つのうち「**閉包レジストリの識別子と表示名（閉包の登録・改名時）**」だけが、I8 の強制対象外になっている。I8 の対象経路一覧は**エントリを生成する経路のみ**であり、閉包の登録・改名は含まれない。**閉包を改名すると索引が古い表記のまま残る**が、それを止める不変条件も点検も無い（§20.5-7 は件数の乖離しか見ない） | I8 とは別に「閉包レジストリの変更時に `entity_index` を同一トランザクションで更新する」を §8.5.1 に置き、経路一覧と同じ形の一覧を持たせる |
| W15 | memory.md:1603 vs 1420-1428 | 3・5 | §22 / §20.5 | §22 が「**深い睡眠の自己内省（§20.5）に項目を足す：`last_reviewed` から一定期間経過、または `model_migration` 発火で self-model 再レビューを通知**」と書いているが、**§20.5 の点検表にその行が無い**（7項目のどれでもない）。`model_migration` 側はレジストリの `model_migration` 行の通知欄で拾えるが、**時間経過側は発火する場所がどこにも無い**。R4／S14 が指摘した「`last_reviewed` を読む主体がいない」が未解消のまま残っている | §20.5 の点検表に「8 self-model の `last_reviewed` 経過」を足す（行き先＝§21 の通知・個別）。§21 には既に行がある |
| W16 | memory.md:667, 750 | 3 | §8.5.2 / §8.5.5 | 「**§6.1-1 の「embedding の有無による分岐」**」「**§6.1-3 の裁定**」と参照しているが、**spec-memory に §6.1 は存在しない**（§6 に小節は無く、見出しは §16.1 のみ）。実体は検証プロトコル（`memory-verification-scenarios.md`）§6.1 の1・3。本文中では自書の §6.1 と読める | 「検証プロトコル §6.1-1」「同 §6.1-3」と外部文書名を明記する |
| W17 | memory.md:667, 922 | 3 | §8.5.2 / §12 経路A | 「**ADR-0010 §5 は「初期は embedding 無しで開始できる」としたが**」「（ADR-0010 §5：初期は embedding 無しで開始しうる）」— **ADR-0010 §5 は `sensitive` 機微フラグの節であり、embedding には一言も触れていない**。該当記述は ADR-0010 の「帰結」（`(+) embedding をスキーマから外したことで…初期実装は embedding 無しで始められる`）。なお ADR-0010 §7 自身も「embedding は対象外（§5）」と同じ誤参照をしている | 参照先を ADR-0010「帰結」に直す。ADR-0010 §7 の `（§5）` も同時に直す（P3：黙って寄せず incident） |
| W18 | memory.md:1382, 1516 | 3 | §20.4 / §20.8-3 / §0.3.1 | 「**§0.3.1-3 の `origin` 強制**」と2箇所が参照しているが、§0.3.1 の番号付きリストは項目が2つしか無く（しかも下記 W19 のとおり両方 `2.`）、**3項目目が存在しない**。かつ `origin` の強制は §0.3.1 ではなく **§0.3.0 の I1** に移されている（節番号のズレ＋移設後の参照放置） | 両箇所を「§0.3.0 I1」に直す |
| W19 | memory.md:181-183 | 7 | §0.3.1 `record()` の書き込み前バリデーション | 番号付きリストが `1. / 2. / 2.` と**番号が重複**。さらに2つ目の `2.`（`scope` の実在）は **I4 の逐語的な再掲**であり、その直後の行が「**ここに再掲しない——再掲すると経路ごとに書く形に戻り、次の経路で漏れる**」と述べている。同じ段落の中で自らの規律を破っている。また冒頭は「`record()` 固有のものが1つある」と書いており、項目が2つあることと矛盾 | 2つ目の項目（`scope` の実在）を削除し、番号を `1.` と地の文に整理する |
| W20 | memory.md:685-694 vs 486-496 | 3・7 | §8.5.3 Stage 2 / §7 候補集合の前段 | §8.5.3 は「**§7「候補集合の前段」の5項目は、本段で実行する**」と書くが、Stage 2 の5行と §7 の5項目は**集合として一致しない**。Stage 2 に **§7-3（アーカイブ層は除外せず解像度で制限）** と **§7-4（`subject[]` を判定に使う／`assemble_brief()` は宛先本人が `subject[]` に含まれるものを除外）** が無く、代わりに §7 の5項目に**無い**「可視性（ADR-0010 §2）」と「不応期（§7 失敗モード表）」が入っている。§7 が「意味論の正典」、§8.5.3 が「実行順序の正典」と役割分担を宣言している以上、**片方にしか無い規則は実行されないか、正典に無いまま実行されるかのどちらか** | Stage 2 の表に §7-3（archive は除外せず、Stage 5 で解像度制限）と §7-4（`mode == brief` のとき宛先本人を `subject[]` で除外）の行を明示的に足し、可視性・不応期の由来を §7 側にも1項目として書く。「5項目」という件数表現をやめる |
| W21 | memory.md:544, 567, 1720 | 7 | §7 失敗モード表 / §7 状態遷移 / A.1-4 | **旧 `θ_admit` が3箇所に残存**し、新語（`θ_ws` / `θ_floor` / 定員 K）と混在している。とくに深刻なのは (a) 状態遷移図が「**入場スコア ≥ θ_admit → 滞在**」だけを入場条件として描いており、**§8.5.5 の二段判定（`rank ≤ K` かつ `s_admit ≥ θ`）と正面から矛盾**すること（絶対閾値単独＝S13-(2) の失敗そのもの）。(b) 居座り対策の「M ターン未参照なら `θ_admit` で入場し直させる」は、どちらの tier の θ か・定員を通すのかが未定義。(c) A.1-4 が `θ_admit` を、A.1-7 が `θ_ws`/`θ_floor` を別々に列挙している | 状態遷移図を二段判定（K と θ）と2 tier（滞在枠・見出し帯）に描き直す。544 を「`θ_ws` と定員 K の両方で入場し直させる」に。A.1-4 から `θ_admit` を削る |
| W22 | memory.md:495, 496, 506, 1026 | 7 | §7 / §13 | line 458 で第1項を `rel_norm` に確定させた**直後の同じ §7 の中で**、495・496・506 が第1項を「関連度」と呼び続けている。§13-1026 も同様。**確定した名前と旧称が同一節内で混在** | 「関連度」を `rel_norm` に置換する（説明として初出のみ併記） |
| W23 | memory.md:1026 vs 743, 776-785 | 3・4 | §13 残る役割 / §8.5.5 | §13 は `strength` の残る役割を「**§7 の入場スコアにおける弱い事前分布（関連度が同点のときのタイブレーカー）**」とするが、(a) `s_admit` の式に `strength` の項が無く、(b) §8.5.5 の「定員内の順序（決定的）」は ピン留め → 保護枠 → 多様性クォータ → `s_admit` 降順の4段で、**同点時のタイブレーク段が無い**。`strength` を読む主体は §14 のアーカイブ降格だけになっており、§13 の記述が空参照 | §8.5.5 の順序に「`s_admit` 同点時は `strength` 降順」を足すか、§13 から当該役割を削除する |
| W24 | memory.md:610 | 4 | §8.5.0 `ranked[]` | **`arms_hit[]` に読む主体が本文のどこにもいない**（文書内で1回しか出現しない）。唯一の読み手であるはずの §7 休眠救済（W10）が「A2 でヒットしたこと」を判定に使うと書きながら、`arms_hit` を参照していない | §7 の免除規則を「`arms_hit` に A2 を含むとき」と書き、Stage 3 の処理として明示する |
| W25 | memory.md:85, 692, 812 | 4 | §8.5.0 / §8.5.3 / §8.5.7 | **`retrieve(query, closure_ctx, mode, budget)` のシグネチャに、Stage 2 と §8.5.7 のゲートが要求する入力が無い。** (a) `sensitive` ゲートは「PO との直接対話（`po_message` 由来のセッション）」かつ「PO がその主体・エンティティに言及したターン」を条件にするが、**セッションの由来もターンの言及情報も引数に無い**（`mode` は context/recall/brief の3値でセッション由来を表現できない。自律ループも `mode == context`）。(b) brief の「宛先本人が `subject[]` に含まれるものを除外」は**宛先職人の識別子**を要するが引数に無い。§20.0 の教訓「シグネチャに落ちた規律だけが機能する」に照らすと、この2つは現状**散文のまま** | `retrieve()` に `session_origin`（`po_message` / `subsystem_kick` / `autonomous`）と `addressee`（brief 時の宛先識別子）を足す。Stage 0 の `subjects[]` を言及判定の入力として明示する |
| W26 | memory.md:85, 202-208 | 4 | §8.5.0 / §0.3 driver が提供するもの | **`closure_ctx` の供給元が未定義。** driver が提供するのは `goal_stack` / `exec_context` / `turn_boundary` の3つで、`exec_context` は「リポジトリ・作業モード等（§11 の文脈一致度に使用）」であって**閉包識別子を含むとは書かれていない**。§8.5.4 の発火条件1「実行文脈の閉包が leaf でない」も、この未定義の上に乗っている。加えて `s_admit` の第2項（文脈一致）は `exec_context` を、第4項（顕著性の目標関連性成分）は `goal_stack` を要するが、**どちらも `retrieve()` に渡らない** | `exec_context` に `closure` を含めることを §0.3 に明記し、`retrieve()` の引数に `exec_context` / `goal_stack` を足す（または `closure_ctx` を「exec_context から導く」と書く） |
| W27 | memory.md:85 | 4 | §8.5.0 / §8.5.5 | **`budget` 引数を §8.5 のどの段も使わない。** 入場は定員 K と絶対下限 θ だけで決まり、§8.5.5 の本文に予算は現れない（レジストリ1482 行だけが「⑤入力＝`s_admit`・予算配分（§6）」と書いている＝表にあって本文に無い、検査項目1）。**定員 K とトークン予算のどちらが binding か**、`K_hl` を予算が下回ったときどう縮めるかが未定義 | §8.5.5 に「定員は上限であり、予算が足りなければ tier ごとに `s_admit` 下位から削る」等の規則を書く。書かないなら `budget` を引数から外す |
| W28 | memory.md:84 vs 813 | 4・7 | §0.3 `recall()` / §8.5.7 | §8.5.7 は recall の定員を「**呼び出し側指定の n**」とするが、§0.3 のシグネチャは **`recall(query)`** で n を受け取れない。`mode` も `budget` も渡せない | `recall(query, n)` あるいは `recall(query, opts)` に直す |
| W29 | memory.md:772 vs 813 | 7 | §8.5.5 tier 表 / §8.5.7 | tier 表の「落選」行は「**非注入（`recall()` で到達可能）**」と書くが、§8.5.0 は「3つの呼び出し元は**同一のパイプライン**を通る」と定め、§8.5.7 の差分表には **recall の絶対下限 θ の行が無い**。recall にも `θ_ws`/`θ_floor` が効くなら落選エントリは recall でも到達できず、効かないならそれが「唯一の差分」表に載っていない。**どちらにせよ未定義** | §8.5.7 に「絶対下限」の行を足す（recall＝θ を適用しない／`θ_floor` のみ、等） |
| W30 | memory.md:806-816 vs 781, 693 | 6・7 | §8.5.7 / §8.5.5 / §8.5.3 | §8.5.7 は「**この表が唯一の差分である**」と宣言するが、**セッション状態に依存する2つの規則が mode 差として書かれていない**。(a) 保護枠（現に滞在中で `a ≥ θ_evict` のものを先に確保）は brief／recall には滞在概念が無いので適用不能。(b) Stage 2-5 の不応期除外は**番頭のセッション状態**であり、これを `brief` に適用すると**職人向けブリーフが番頭のワーキングセット履歴で削られる**（明らかに誤り） | §8.5.7 に「保護枠」「不応期」の行を足し、brief では両方無効と書く |
| W31 | memory.md:1085 vs 121 | 7 | §15 予測符号化 / §0.3.0 I8 | §15 は「**予測誤差が小さい入力は埋め込み生成・更新をスキップ**」を §8.5.6 索引の更新（I8）を挿入先として指定するが、**I8 は「索引を同一トランザクションで更新する。対象は全アームの索引と `entity_index`」と無条件で定めており、I6 が違反を failed とする**。スキップは I8 違反そのもの。§15 の「スキップしても A1・A2 は動く」という緩和は、I8 の「全アームの索引」と両立しない | I8 に「A3（埋め込み）索引は §15 のスキップ規則の対象とし、`embedded_by` の欠落として索引側に記録する（§20.5-7 の乖離検出が拾う）」という例外を明記する。例外を書かないなら §15 の当該行を撤回する |
| W32 | memory.md:723 vs 18 | 7 | §8.5.4 / 冒頭の位置づけ | §8.5.4 は ADR-0010 §2 の「祖先に属する記憶が候補になる」を「**既定と読み替え**」て子孫方向へ拡張するが、**本書冒頭（line 18）は「本書 Part I と食い違う場合は ADR-0010 が優先する」と定めており、ADR-0010 §2 は未修正**（子孫・ファンアウトの記述は ADR に一言も無い。grep 確認済み）。したがって**正典に照らすと横断ファンアウトは無効**という読みが成立する。加えて P3 は「黙って寄せず **incident を積む**」を要求するが、§8.5.4 は「正式化フェーズで参照を足す」と書くだけで**incident は積まれていない**（`docs/` に該当 incident 無し） | ADR-0010 §2 に本項への参照を足す（P3 の要求どおり incident を積んだうえで）。それまでは §8.5.4 の冒頭に「ADR-0010 §2 が更新されるまで有効化しない」を実装順序の制約として書く（Memory 面と同じ扱い） |
| W33 | memory.md:857 vs 813, 816 | 7 | §9 割当規則 / §8.5.7 | 三つ巴の未裁定。§9 は「**アーカイブ層のエントリは tier が resident でも見出しに落とす**（§7-3）」と定め、§8.5.7 は「**`brief` は見出し帯を使わない。職人は展開ツールを持たない／ブリーフに載らなかったものは職人に一切届かない**」と定める。両方を適用すると、**brief にアーカイブ層のエントリが「見出し（〜10トークン）」だけで載り、職人はそれを展開できない**＝役に立たない断片が発注文に入る。§9 は `sensitive` × 割当規則の優先だけを裁定し、アーカイブ × brief を裁定していない | §9 に「`mode == brief` ではアーカイブ層のエントリは要約で載せる、または候補から外す」を足す |
| W34 | memory.md:427-436, 787-793 | 4・7 | §6 配置原則 / §8.5.5 見出し帯 | **見出し帯がプロンプト上のどこに置かれるかが §6 の配置原則に無い**（[静的]／[準静的] ワーキングセット／[動的] 今ターン固有／[動的] 会話履歴の4層のみ）。かつ **見出し帯には滞在スコア・最小滞在・保護枠・ヒステリシスが一切適用されない**（すべて resident tier の概念）ため、`K_ws` より一桁大きい帯が**毎ターン再選抜されて並びが変わる**。§6 は「プレフィックスキャッシュが3つの予算のうち実務では最大」と述べており、§8.5.5 の「見出し帯を広げても失うのはトークンだけ」という主張と矛盾する | §6 の配置原則に見出し帯の位置（準静的の直後／動的の先頭）を足し、見出し帯にもヒステリシス（最小滞在 or 順序固定）を与えるか、「毎ターン失効する」ことを明示的に受容すると書く |
| W35 | memory.md:693 vs 787-793 | 7 | §8.5.3 Stage 2-5 / §8.5.5 | Stage 2 の不応期フィルタは **RRF より前**にあるため、退場直後の記憶は**見出し帯にも出てこない**。§8.5.5 は見出し帯を「見逃しの受け皿」「§0.8 の重み付けは見出し帯の広さで表現する」と位置づけており、目的と矛盾する（不応期は「入退場の点滅を防ぐ」ためのものであって、存在を隠すためのものではない） | 不応期の除外を滞在枠（resident tier）だけに限定し、見出し帯には適用しないと Stage 4 側に書く |
| W36 | memory.md:649 vs 619 | 3・4 | §8.5.1 / §8.5.0 | §8.5.1 の聞き返し発火条件が「`unresolved[]` が空でなく、かつ **入場後**の候補が閉包をまたいで散らばるとき」— **Stage 0 の機構でありながら Stage 4 の結果を条件にしており、パイプラインの順序として閉じていない**（レジストリ1477 行も発火を「候補生成の Stage 0」と書く）。さらに `retrieve()` の返り値は `{ ranked[], trace }` だけで、**「聞き返せ」を分身に伝える項が無い**。「判定はルールベースで行い番頭の裁量にしない」という規律が、伝える手段の欠落で成立しない | 聞き返し判定を Stage 4 の後段（Stage 6 相当）に置き、`retrieve()` の返り値に `clarify: { candidates[] } \| null` を足す。レジストリの発火欄も直す |
| W37 | memory.md:89, 93 | 4 | §0.3 | **呼び出し元の無い公開 API が3つある。** (a) `pin()` / `unpin()` — §12 が「訂正」を機械的に定義し §7 が「このセッション中のユーザーの訂正はピン留め」と書くが、**`pin()` を呼ぶ主体・タイミングが本文のどこにも無い**（レジストリの点火行の出力も再検証キュー止まり）。(b) `changes_since(cursor)` — 「driver が点火と再検証を組み立てるための読み取り口」とあるが、§20.4 の積む条件はすべてイベント駆動で、**`changes_since()` を使う機構が1つも無い**。(c) `reindex(scope_or_all, arm)` — §8.5.6 もレジストリも API 名を挙げず、`arm` の**値域が未定義**（`entity_index` はアームではなく Stage 0 の索引なので、再構築対象に指定できるのか不明） | (a) 点火の②に「候補ありなら `pin()` を呼ぶ」を足す。(b) 使う機構が無いなら §0.3 から削るか、緊急無効化チャネル（§19）の入力として配線する。(c) `arm` の値域を `A1..A5 \| entity_index \| all` と書く |
| W38 | memory.md:127-136 | 4 | §0.3.0 派生エントリのフィールド合成規則 | 合成規則の表に **`strength` と `salience` が無い**。両者は永続フィールドで、`s_admit` の第4項（顕著性）と §14 のアーカイブ降格が読む。**蒸留・統合が生成したエントリの `salience` を誰がどう決めるかが未定義**のまま、§8.5.5 の入場判定がそれを入力にしている。ADR-0010 §7 は「`salience`・`strength` は書き込んだ時点のモデルの主観的判断」と述べており、合成時にも同じ問題が起きる | 合成規則の表に `strength`（出典の最大値 or 初期値にリセット）と `salience`（同）の行を足す |
| W39 | memory.md:1477-1487 vs 1355 | 6 | §20.7.1 / §20.3 | §20.3 は「同期の機構は **`—（同期。キューに乗らない）` と書き、空欄にしない——空欄は「埋め忘れ」と区別できない**」と定めるが、レジストリで実際にそう書かれているのは候補生成パイプライン行だけ。**同じく同期で走る6行**（エンティティ索引の解決・横断ファンアウト・二段照会・入場の二段判定・解像度の割当・索引の更新）の優先度欄は素の `—` で、§20.3 が禁じた「埋め忘れと区別できない」状態のまま | 6行の優先度欄を `—（同期。キューに乗らない）` に統一する |
| W40 | memory.md:778-783 | 7 | §8.5.5 定員内の順序 | 「`s_admit` 降順で埋める前に、**2つの枠を先に確保する**」と書いた直後に **3項目**（ピン留め・保護枠・多様性クォータ）を列挙している。件数が本文と一致しない | 「3つの枠」に直すか、ピン留めは定員外なので「2つ」の外にあると明示する |

## 検査したが問題が無かった項目

以下は実際に突き合わせて**指摘に至らなかった**もの。ここに挙げていない領域は「見ていない」と読むこと。

- **§0.3.0 の対象経路一覧と I8**：`record()` / `propose()` / `supersede()` / 蒸留・昇格・統合 / チャンキング / tombstone の新版作成の6経路すべてが「I1〜I8 を通す」になっており、見出しの範囲表記も `I1〜I8` に更新済み。§0.3.1 の 3項目目も `I1〜I8` を列挙（8項目分ある）。**R5 が指摘した「経路一覧の欠落」は解消している**（ただし W6：レジストリ側の未登録は別問題）
- **§7-1〜§7-5 の項番**：§7「候補集合の前段」の5項目の番号と、本文各所からの `§7-1`〜`§7-5` 参照は**すべて一致**（1=superseded、2=tombstone、3=archive、4=`subject[]`、5=`sensitive`）。参照元は §8.5.2:673・681、§8.5.3:688-693、§8.5.7:811-812、§9:857。**節番号のズレは無い**（ただし W20：Stage 2 の集合の食い違いは別問題）
- **§20.5-7（索引の健全性）の3表への登録**：§21 の通知表（1585 行「索引と本体の乖離が閾値を超えた／§20.5-7／ダイジェスト」）・§20.7.1（1484 行）・§20.5 の点検表（項目7）に揃って存在する。**§20.3 の優先度表だけが欠けている**（→ W5）
- **`tier` / `entity_index` / `k_arm` の読み手**：`tier` は §9 の割当規則が、`entity_index` は §8.5.2 の A2・§12 経路A・§0.3.0 I8・§20.5-7 が、`k_arm` は §8.5.2・§15・A.1-7 が読んでいる。**読む主体がいないのは `arms_hit[]`（W24）・`budget`（W27）のみ**
- **`trace` の読み手**：§16 可観測性が「実体は `retrieve()` が返す `trace`」と明記し、§8.5.3 が「落ちた件数と理由を記録する」、§8.5.8 が肥大対策を書いている。保持場所（セッション側）も §2 の規律に揃っている（ただし W4：Memory 面での提示との整合は別問題）
- **`sensitive` × `assemble_brief()` の R3 積み残し**：§7-5 の1つ目のサブ項目が「`assemble_brief()` は除外の対象外とし §0.3.1 の解像度規則に委ねる」と明記し、§8.5.7 の表・§9:856 と3箇所で一致している。**R3 が指摘した正面矛盾は解消**
- **§13 と §7-3 の「検索コスト」矛盾**：§13:1029 が当該役割を明示的に撤回し、§8.5.2 の二段照会へ移している。**S13-(6) は解消**
- **ADR-0010 の Substrate 定義と §8.5 が使うフィールド**：`subject[]`（§8）・`external_ref[]`（§4）・`sensitive`（§5）・`context.scope`（§2）はいずれも ADR に定義があり、§8.5.1 の供給元表と一致する
- **§20.3 の「本表の対象はキューに入る作業だけ」注記**：`retrieve()` / `collision_candidates()` / `resolve_entities()` が起床源でも作業種別でもないことを明記しており、この注記自体は整合している（→ 適用漏れは W39）
- **`fact_type` / `origin` / 矛盾解決規則**：§12 の解決規則表は ADR-0010 §1.1・§1.3 と一致（origin → fact_type の順、3値）。§8.5 の新設で崩れた箇所は見当たらない
- **`θ_ws` / `θ_floor` / `K_ws` / `K_hl` の定義箇所**：§7 ヒステリシス（474-482）と §8.5.5 の tier 表（768-774）で二重に定義されているが、値も上下関係も矛盾していない
- **未検査の領域**：§10（会話履歴の圧縮）／§17（内部状態）／§22〜§25（Governance）／付録 B・C は、§8.5 との配線という観点で読んだだけで、内部の整合は検査していない。ADR-0010 は §8.5 が参照する箇所（§1〜§5・§7・§8・帰結）を読んだが、§0・§6 の記述と spec 本文の対応は未検査

## 総括

指摘件数：**40件**

種別別の内訳（複数種別に該当するものは主たる種別で集計）：

| 種別 | 内容 | 件数 | 該当 |
|---|---|---|---|
| 1 | レジストリの欄が本文で定義されていない | 5 | W1・W2・W3・W4・W5 |
| 2 | レジストリに未登録の機構 | 9 | W6〜W14 |
| 3 | 宙ぶらりんの参照・節番号のズレ | 5 | W15・W16・W17・W18・W36 |
| 4 | 能力はあるが呼び出し元が無い | 7 | W24・W25・W26・W27・W34・W37・W38 |
| 5 | 検出はするが対応が無い | 0 | （W15 は 3 で集計） |
| 6 | 層をまたいで配線されていない | 2 | W30・W39 |
| 7 | 相互に矛盾する2つの記述 | 12 | W19・W20・W21・W22・W23・W28・W29・W31・W32・W33・W35・W40 |

**構造的な所見**（3点）。

1. **種別4（呼び出し元が無い）が §8.5 に集中している。** §8.5 は「実行層・シグネチャに落とす」規律のもとに書かれているが、`retrieve()` のシグネチャが Stage 2・§8.5.7・`s_admit` 第2〜4項が要求する入力を**受け取れていない**（W25・W26・W27）。とくに `sensitive` ゲートの「PO 直接対話かつ言及ターン」は、**引数が無いので実装できない**。§20.0 と検証プロトコル §6.4 が最重要とする失敗パターン（散文だけで実行層に落ちていない）の現物が、§8.5 の内部に少なくとも2つある（W10 の休眠救済免除、W25 の `sensitive` ゲート）。
2. **配線レジストリの運用が「新規機構だけ」に偏っている。** §8.5 が導入した8機構は全部登録された一方、**既存の中核機構（§0.3.0 I1〜I7、話題転換の検出、睡眠の発火、`propose()` の呼び出し）が未登録のまま**（W6〜W9）。レジストリは「機構を足す＝1行を足す」ための道具として使われており、**既存機構の棚卸しが一度も行われていない**。レジストリの網羅性を主張するなら、一度全機構を洗い出す必要がある。
3. **§7 と §8.5 の役割分担（意味論の正典／実行順序の正典）が、集合として一致していない**（W20）。分担そのものは正しいが、**「同じ5項目」という前提が事実に反する**まま両節が相互参照しており、§7-3・§7-4 が Stage 2 から落ちている。分担を宣言するときは、両側の集合が一致することを機械的に確認できる形（項目に安定したIDを振る等）にしないと同じズレが再発する。
