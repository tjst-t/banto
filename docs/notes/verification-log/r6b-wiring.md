---
id: verification-log-r6b-wiring
type: note
status: draft
refs: [spec-memory, spec-memory-contracts, adr-0010, memory-verification-scenarios]
---

# R6b 配線チェック（契約層の分離と §20.8 の D3 修正の後）

**対象**: `docs/spec/memory.md` / `docs/spec/memory-contracts.md` / `docs/adr/adr-0010-memory-substrate-schema.md` / `docs/notes/memory-verification-scenarios.md`（いずれも working tree、`git show HEAD:...` と突き合わせ済み） / **検査者**: R6b 配線チェック専任エージェント / **検査項目**: 検証プロトコル §2.4 の1〜7

シミュレーションは行っていない。静的な参照解決のみ。

## 1. 再編で壊れた配線

| # | 箇所（ファイル:行） | 種別(1-7) | 該当節 | 内容 | 修正案 |
|---|---|---|---|---|---|
| 1 | `docs/spec/memory.md:594`, `:710` | 3 | 検証プロトコル §6.1 | `memory.md` が「検証プロトコル §6.1-1」（594行目、embedding 分岐の根拠）と「§6.1-3」（710行目、θ_admit を絶対値のまま残す裁定の根拠）を引用している。この番号は **R5 当時の `docs/notes/memory-verification-scenarios.md` §6.1**（「次にやること（R5の主題）」の1〜5番の箇条書き。1=候補生成パイプライン、3=θ_admit を絶対値/相対どちらにするか）を指していた。しかし今回のノート再編で §6 は全面的に書き換わり、**現在の §6.1 は「次にやること（R7 の主題）」＝深い睡眠と再索引のスケーリングの箇条書きに置き換わっている**（現 §6.1 の3番目は「再索引の分割単位・順序・優先順位」であり、θ_admit とは無関係）。旧 §6.1 の中身は現在 §6.2「決着済み」に1段落へ圧縮されて移設されており、**「§6.1-1」「§6.1-3」という番号付き参照は存在しない箇条書き項目を指す宙ぶらりん参照になっている** | 両箇所の参照先を「`memory-verification-scenarios.md` §6.2『検索機構（R6）』」に張り替える。番号付き参照（`-1`/`-3`）は削る |
| 2 | `docs/spec/memory.md:545` | 3 | §20.8 | 「閉包レジストリは...名前から引く手段が無かった（**§20.8 の R3 残件**）」。しかし今回の再編で §20.8 は「経緯を削って残件一覧の表（U1〜U19）だけ」に作り直されており、**ラウンド番号でラベル付けされた残件（「R3 残件」）はもう §20.8 のどこにも存在しない**。当該経緯（エンティティ名→閉包ID逆引き索引の不在）は `memory-verification-scenarios.md` §7「R3」に移設済み | 「（§20.8 の R3 残件）」を「（`memory-verification-scenarios.md` §7 の R3。現 §20.8 では U8 の一部として解消済みと記録）」等に張り替える |
| 3 | `docs/spec/memory.md:987` | 3 | §20.8 | 「その結果を `supersede()` へ流す手順が書かれていなかった（**§20.8。2ラウンド連続の指摘**）」。「2ラウンド連続」という経緯情報は現行の §20.8（U1〜U19 の表のみ）からは検証できない。これも旧 §20.8 の経緯記述（今は `memory-verification-scenarios.md` §7 側）を前提にした文言が本文に残ったもの | 「（§20.8。2ラウンド連続の指摘）」から経緯の主張を落とすか、`memory-verification-scenarios.md` §7 の該当ラウンドへ張り替える |
| 4 | `docs/spec/memory.md:1189` | 3 | §16.1／§20.8 | 「ADR-0010 §6 と §20.8 で2回行った手続き」「§20.8 末尾の限界（シナリオが見ていない領域は2回とも見つからない）」。**この「限界」の文章そのものが今回の再編で `memory-verification-scenarios.md` §2.5 へ移設されている**（同ノートの diff に「旧 `spec-memory` §20.8 末尾より移設」と明記されている）。現在の §20.8 末尾には U1〜U19 の表と「対処済みで未再検証のものは本表に載せない」という一文しかなく、「限界」の記述は無い | 「§20.8 末尾の限界」を「`memory-verification-scenarios.md` §2.5 の限界」に張り替える |
| 5 | `docs/spec/memory.md:1311` | 3 | §20.0／§20.8 | 「この原則は driver の外でも成り立つ（**§20.8 の検証で確認**）」に続けて「同じ10シナリオを設計前後で回した結果...」という R1/R2 のラウンド比較の記述がある。この比較の実体（R1→R2 で何が解消したか）は現在 `memory-verification-scenarios.md` §7 の R1・R2 セクションにあり、**§20.8 には無い**。§20.8 を読んでも「検証で確認」した中身に辿り着けない | 「（§20.8 の検証で確認）」を「（`memory-verification-scenarios.md` §7 の R1・R2 で確認）」に張り替える |
| 6 | `docs/spec/memory.md:18-21` | 7 | 冒頭「構成」表 | 冒頭の構成表は `spec-memory-contracts` の中身を「**§0.3 / §0.3.0 / §0.3.1**」とだけ記す。しかし今回の再編で §0.3.1 配下にあった「ハーネスとの関係」「規律: Substrate に開発固有の語彙を入れない」が `### 0.3.2` `### 0.3.3` として独立節に昇格しており、`memory-contracts.md` 側にも実在する（同ファイル21〜32行目の「本書が正典であるもの」表に明記）。**同じ `memory.md` 内の §0.3 ポインタ節（95行目）は §0.3.2・§0.3.3 を正しく含めている**のに、冒頭の構成表だけが更新されておらず、2つの表が食い違っている | 冒頭の構成表の該当セルを「§0.3 / §0.3.0 / §0.3.1 / §0.3.2 / §0.3.3」に更新する |
| 7 | `docs/adr/adr-0010-memory-substrate-schema.md:232` | 7 | 帰結節 | 「(−) spec-memory の本文を同時に直す必要がある...**§25 と §2 の物理削除条件の不一致、および §2 が参照する存在しない『§26』**」を、今なお「まだ直っていない」帰結として列挙している。しかし今回の再編で `memory.md` §2 はまさにこの2点を訂正済みである（`memory.md:31` の取り消し線注記、および `memory.md:210` の訂正文）。ADR-0010 の帰結節と `memory.md` の現状が正面から食い違っている | ADR-0010 §帰結の当該項目に「2026-07-26 spec-memory §2 で解消済み」等の注記を足す。ADR 本文を書き換えないなら、少なくとも二重管理にならないよう解消済みである旨を追記する |

## 2. 再編と無関係な指摘

（無し。今回の検査は再編に起因する観点に絞って実施した範囲では、上記7件以外に静的参照の破れは見つからなかった。）

## 3. 検査したが問題が無かった項目

- `memory.md` 本文中のすべての `§0.3` / `§0.3.0` / `§0.3.1` / `§0.3.2` / `§0.3.3` 参照（45箇所）は `memory-contracts.md` に実在する節へ解決する。逆方向（`memory-contracts.md` から `memory.md` への `§0.2, §0.8, §0.9, §1, §12, §13, §14, §16, §18, §19, §20.0, §20.2, §20.4, §20.7.1, §3, §4, §7, §8, §8.5 系, §9` 等の参照）もすべて `memory.md` に実在する見出しへ解決する
- `memory-contracts.md` の「本書が正典であるもの」表（21〜32行目）は同ファイルの実際の見出し構成（§0.3・§0.3.0・§0.3.1・§0.3.2・§0.3.3）と一致している
- `§0.3` ポインタ節自体（`memory.md` 82〜96行目）の見出し階層（`###` のまま）は移動先 `memory-contracts.md` の見出し階層（`### 0.3` → `### 0.3.0` → `#### ...` → `### 0.3.1` → `### 0.3.2` → `### 0.3.3`）と整合しており、見出し階層の破れは無い
- `§26` への参照は現行4文書中どこにも残っていない（`memory.md:31`・`:210` の取り消し線注記のみ、いずれも「解消済み」の記録として正しい）。`§25` と `§2` の物理削除条件（法規制対応のみが唯一の例外）は現在一致している
- `U1`〜`U19` の残件表は、`git show HEAD:docs/spec/memory.md` の旧 §20.8（経緯付き・番号なし残件列挙）および旧「R3で新たに判明した残件」の各項目とすべて突き合わせたが、**規範的記述・未解決項目の脱落は無い**。旧 §20.8 の7項目（検出の起点／散文規律／lineage-aware／プッシュ型の気づき／procedural裁定／activity ログ／本節内の不整合6件）と旧「R3残件」9項目はいずれも「本文で解消済み」または「U1〜U19 のいずれかに引き継がれている」のどちらかに分類できた（例：プッシュ型の気づき→U11、driver活動ログ→U12、procedural裁定→U9、`external_ref.system` に web が無い→U10、出典の水増し→U15、閉包レジストリのライフサイクル→U8、エクスポート→U7）
- 本文中の `U12` への3箇所の参照（`memory.md:576, 859, 1464`）はいずれも U12 の実際の記述（driver 活動ログの置き場が未設計。`alias()` の監査ログ・`via_fanout` の監査ログの2件が参照元として増えた）と整合している
- `memory-verification-scenarios.md` §6.3 は `spec-memory` §20.8 を残件一覧の唯一の正典とするポインタに変わっており、二重管理は解消されている（旧版で両方に同じ一覧を持っていた状態は解消済み）
- `spec` と `ADR-0010` の間の二重管理は、§7（表2.1 追記の「読むことと書くことは別」節）を含め、明示的な「ADR が正典」の宣言で解消されている（§帰結節の1件を除く。§1 の #7 参照）

## 4. 総括

指摘件数：**7件**（1章＝7件／2章＝0件）。種別内訳：種別3（宙ぶらりんの参照）＝5件（#1〜#5）、種別7（相互矛盾）＝2件（#6・#7）。

すべて今回の再編（`§0.3` 系の契約層への分離、`§20.8` の残件一覧化、参照の張り替え、`memory-verification-scenarios.md` §6 の全面書き換え）に起因する。契約層分離そのもの（`§0.3`/`§0.3.0`/`§0.3.1`/`§0.3.2`/`§0.3.3` の相互参照、見出し階層、移動先の自己完結性）には破れが無く、設計どおり機能している。破れは**すべて「参照先の中身が別物に置き換わったのに、参照元の文言（節番号・経緯の主張）が旧内容を前提にしたまま残った」という同型のパターン**——`§20.8` の経緯記述の除去（#2〜#5）と `memory-verification-scenarios.md` §6 の全面書き換え（#1）の2箇所が、本文側の参照更新を伴わずに行われたために生じている。#6・#7 は本文の食い違いというより「片方の表・記述を更新し、対になるもう片方を更新し忘れた」という配線忘れの典型形。
