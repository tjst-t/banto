# 2026-09-02 仕様書の分割と「追記ログ化」クリーンアップ

`docs/specs/v4-architecture.md`（3,093行）が、CLAUDE.mdの規律
「仕様書は決まったことだけを書き、更新する（追記ログにしない）」に違反して
取り消し線・訂正履歴が本文に大量に残っている状態だった（Opusレビュー C1）。
ユーザーの提案で、クリーンアップと同時にファイルを適切な粒度に分割した。

## 分割

| ファイル | 中身 | 行数（分割後） |
|---|---|---|
| `docs/specs/v4-architecture.md` | §1・§2.1〜2.6・§3・§4・§8・§9・§10（骨格として残す） | 約2,300行 |
| `docs/specs/v4-security.md`（新設） | 旧§2.7セキュリティ境界 | 約230行 |
| `docs/specs/v4-frontend.md`（新設） | 旧§6フロントエンド要件（内部の§6.0〜§6.6の番号は維持——他文書からの参照を壊さないため） | 約585行 |
| `docs/specs/v4-modules.md` | 変更なし（内容）、外部参照のみ更新 | 685行 |

**§10「まだ決まっていないこと」は分割しなかった**——当初の計画（各新ファイルに
自分のトピックの未決事項だけを持たせる）を検討したが、番号を再配置すると
他文書・notesからの`item N`参照が壊れるリスクが高く、regle3（真実は一箇所）
の観点でも「1つの番号付きリストがどこか1箇所にだけある」方が安全と判断し、
**§10は`v4-architecture.md`に残し、`v4-security.md`・`v4-frontend.md`からは
`アーキ仕様 §10 item N`の形で参照するだけに留めた**。

**外部参照の更新**：`v4-modules.md`・`requirements.md`・`mock/README.md`内の
「アーキ仕様 §2.7」「アーキ仕様 §6.x」を、それぞれ`v4-security.md`・
`v4-frontend.md`への参照に機械的に置き換えた。

## クリーンアップで本文から削った訂正履歴

以下は「もう決着していて、今後蒸し返される見込みが無い」訂正の経緯として、
本文からは削り、この節に保存する。

### 1. Elicitation MRTR（`InputRequiredResult`）の誤り

旧本文：「MRTR（`InputRequiredResult`に`elicitation/create`を入れて返し、
`inputResponses`付きで呼び出しを再送する）は当初この文書が確定事項として
書いていたが、誤りだった（規則1・規則8）——`@modelcontextprotocol/sdk@1.30.0`に
`inputResponses`/`InputRequiredResult`は存在しない（grep 0件）。」

現在の本文には、この誤りの経緯を削り「elicitInput()の呼び出しは同期的なもの
だけ」という結論だけを残した。

### 2. タイムアウト検知手段の誤り

旧本文：「訂正（2026-08-31、規則1）：banto（client）側にタイムアウト検知の
手段が『無い』と当初この文書に書いたが、誤りだった。」以下、`signal`の存在に
気づかなかった経緯（`run-timeout.mjs`が第2引数を受け取っていなかった）を
説明していた。

現在の本文には、`signal: AbortSignal`が実際に渡っていて機能する、という
結論と実測ログだけを残した。

### 3. 受信箱3状態表の「訂正版」表記

「決定・2026-08-31、訂正版」「という点が当初の設計（常にタイムアウト済み
前提）からの主な改善」という、旧設計との対比の枠組みを削り、現在の3状態表
そのものは残した（これは決定内容そのものなので削らない）。

### 4. stdio 2重起動の「旧文言」注記

「旧文言は対処をitem 9に持ち越していたが決着した」という、以前の書き方への
自己言及を削除。現在の本文には解決策（Runner を実 Module に直接繋がせない）
だけを残した。

### 5. control-plane/data-plane 分離の撤回

旧本文は「旧案（2026-09-01決定）——呼び出し前にcoreへ許可だけ求め
（control-plane）、許可後は実データをModule同士が直接やり取りする
（data-plane）——は撤回する」という形で、撤回した旧案の名前・決定日を
詳しく書いていた。

**現在の本文には、採用しなかった理由（transportが要る・なりすまし対策が
要る）という決定の根拠は残し**、旧案の名前・撤回の経緯（いつ・何を）は
このノートに移した。

### 6. role依存の3段カスケード案

「当初Vaultについて『instance既定接続→Project上書き→呼び出し元Module自身の
設定』という3段カスケードをbanto側の機構として設計しかけたが（旧決定・
2026-09-01『Vault接続の指定とカスケード』、下記で置き換え）」という経緯を削除。
現在の本文には「banto が代わりに決める仕組みは持たない」という結論と、
その理由（Module ごとに選び方が違う、独立性と矛盾する）だけを残した。

### 7. Vault接続のアドレス——カスケード部分の撤回

見出しの「カスケード部分は2026-09-02に撤回——下記」という自己言及と、
`~~選び方は§2.2「設定のカスケード」と同じ3段~~ → 撤回。`という取り消し線
段落を削除。現在の本文には「持たない」という結論に統合した文だけを残した。

### 8. 代理サーバ方式の訂正（createSdkMcpServer→低レベルServer）

「決定・2026-09-02、`poc/06-resource-prompt-relay/`で訂正。当初はClaude Agent
SDKの`createSdkMcpServer`を想定していたが」という訂正の経緯を削除。**「tool
しか受け付けない高レベルラッパーだから使わない」という判断根拠は残した。**

### 9. §2.9「決まっていないこと・気づいた穴」の番号付きリスト

4件中3件（Memory書き換え・Event Store保管上限・Fork時のMemory食い違い）が
すでに決定済みで取り消し線付きだった。**決定済みの3件はリストから削除**
——それぞれの決定は§2.1・§2.2に既に書かれているので、ここでの重複した
参照は不要と判断した。残った1件（`references/`の重さが見えていない）だけを
「まだ決まっていないこと」として残した。

### 10. サブエージェントの会話の置き場——訂正ブロック

「訂正（2026-08-31、規則1・8）：以前この文書は『サブエージェントの専用
Threadは普通のThread』『Moduleの中に隠れた別世界を作らない』と書いていたが、
誤りだった。§10 item 8を『まだ決まっていない』と書きながら、同じ問いに
この箇所だけ先走って答えを出しており、文書内で矛盾していた」という、
文書内矛盾の発見経緯を削除。現在の本文には結論（Subagent Moduleの持ち物）
だけを残した。

### 11. §5.5「Module同士の呼び出し」の墓標

節まるごと「この節は旧い（直結モデル）。§2.5で撤回・置き換え済み」という
説明文だった。**節番号（§5.5）は欠番のまま残し**（`v4-modules.md`が§5.6/§5.7を
参照しているため番号をずらせない）、中身は「§2.5を参照」という1行の
引用ブロックに縮めた。

### 12. §8「v4で最初に潰す実測」の取り消し線と改題

A-1〜A-9・B-1・B-2の全11行が`~~見出し~~ **→ 済（日付）。結果**`という
「取り消し線＋済マーク」の形式だった。見出しを「実測で分かったこと」に
変更し、取り消し線と「→済（日付）」という完了マークの言い回しを削って、
「測ったこと／結果」の2列表に整理し直した（内容は失っていない——測った
問いと結果、両方をそのまま残した）。

## 判断に迷い、削らずに残した箇所

- §2.1「read model の方式を決めた（item 4、決定・2026-08-30）」のような
  **決定見出し**——item番号と日付は参照IDとして機能しているので残した
- §10.0〜10.2 の取り消し線（`~~18 Configuration の形~~ 済`等）——**当初の
  クリーンアップ対象に含めていたが、見直した。** これらは「まだ決まって
  いないこと」という節の性質上、番号付きチェックリストの完了状態を示す
  ためのものであり、他の箇所（§10本体の番号付きリスト自体）でも同じ
  「取り消し線＋→決定」の形式を一貫して使っている。**「追記ログ」ではなく
  「進捗チェックリスト」として機能しているため、削らないことにした**
- §2.4.1「設計意図とのズレ（確認・2026-08-31）」——今も実装者が読むべき
  Elicitationの想定ズレのトレードオフなので残した
- §9の「SDK組み込み資源系tool」行の但し書き（v3知見の限定条件）——生きた
  限定条件なので残した
- §3末尾のMemory関連の記述——確認したところ、既にitem 3の決定を正しく
  参照する形になっていたため、追加の修正は不要だった

## 破棄

このノート自体は破棄しない（過去の記録として残す）。
