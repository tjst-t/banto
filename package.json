{
  "name": "banto",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*",
    "modules/*"
  ],
  "scripts": {
    "build": "tsc --build packages/core packages/module-kit packages/runner packages/observer packages/factory modules/fs modules/shell modules/repo modules/env-process modules/env-docker modules/publish-none modules/env-script modules/worker modules/ledger apps/host && npm run build:web",
    "build:web": "npm run build --workspace apps/web",
    "typecheck": "tsc --build packages/core packages/module-kit packages/runner packages/observer packages/factory modules/fs modules/shell modules/repo modules/env-process modules/env-docker modules/publish-none modules/env-script modules/worker modules/ledger apps/host --force",
    "test": "npm run build:web && vitest run",
    "clean": "tsc --build packages/core packages/module-kit packages/runner packages/observer packages/factory modules/fs modules/shell modules/repo modules/env-process modules/env-docker modules/publish-none modules/env-script modules/worker modules/ledger apps/host --clean"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "playwright": "^1.62.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.21.5": true
  }
}
