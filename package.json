{
  "name": "banto",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*",
    "modules/*"
  ],
  "scripts": {
    "build": "tsc --build packages/core packages/module-kit packages/runner packages/observer packages/factory modules/fs modules/shell modules/repo modules/env-process modules/publish-none modules/env-script apps/host && npm run build:web",
    "build:web": "npm run build --workspace apps/web",
    "typecheck": "tsc --build packages/core packages/module-kit packages/runner packages/observer packages/factory modules/fs modules/shell modules/repo modules/env-process modules/publish-none modules/env-script apps/host --force",
    "test": "vitest run",
    "clean": "tsc --build packages/core packages/module-kit packages/runner packages/observer packages/factory modules/fs modules/shell modules/repo modules/env-process modules/publish-none modules/env-script apps/host --clean"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.21.5": true
  }
}
