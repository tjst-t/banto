{
  "name": "banto",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*",
    "modules/*"
  ],
  "scripts": {
    "build": "tsc --build packages/core packages/module-kit packages/runner packages/observer modules/fs modules/shell modules/repo apps/host",
    "typecheck": "tsc --build packages/core packages/module-kit packages/runner packages/observer modules/fs modules/shell modules/repo apps/host --force",
    "test": "vitest run",
    "clean": "tsc --build packages/core packages/module-kit packages/runner packages/observer modules/fs modules/shell modules/repo apps/host --clean"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
