# banto

**人に伴走し、人の助けとなるソフトウェア。** 第一の領域はソフトウェア開発。

## まず読む

1. `docs/vision.md` — 何のためのものか
2. `docs/requirements.md` — 要件（出所つき）
3. **`docs/specs/v4-architecture.md` — v4 の仕様。決まったことはここにしか無い**
4. `docs/specs/v4-modules.md` — Module 一覧（3 の §5 の下にぶら下がる）
5. `docs/lessons.md` — 前の実装で得た教訓

**前の実装（`main` と `v3`）とその ADR は、このワークツリーに持ってきていない。**
生きていないものを手元に置くと、いつのまにか根拠にしてしまうため。
どうしても見たいときは `~/worktrees/banto-v3` に残っている。

## 現状

**2026-08-29、v3 を捨てて v4 を作ることが決定した。**
理由：v3 は「Module の面を MCP で区切る」という原理が固まる前に書かれたもので、
core と Module の境界が中途半端なまま歪みを生み続けていた。

**v3 から拾うのは知見だけ。コードは持ってこない。**
「v3 の実装がこうなっている」は、v4 がどうあるべきかの根拠にならない。
引き継ぐ知見は `docs/specs/v4-architecture.md` §9 にまとめてある。
これは前の実装（`main`）に対する姿勢——**部品として引くのはよいが、土台にはしない**
——を、v3 にも適用するということ。

**旧 ADR も生きていない。参考にすることはあっても、アーキテクチャの決定は
ゼロから実施する**（2026-08-29 決定）。あの決定群は「Module の面を MCP で区切る」
という原理が固まる前に、v3 の実装と一体で育ったもの。
**「決定N がこう言っている」を根拠にしない。**
拾いたい決定があるなら `docs/specs/v4-architecture.md` で**決め直して、そこに書く**
——同じ結論になってもよいが、根拠は旧 ADR ではなく、いま示せる理由。
**そこに書かれていない決定は、まだ存在しない。**

**まだ決まっていないこと**が多い。決まっていないものを、決まっているかのように扱わない
（一覧は `docs/specs/v4-architecture.md` §10）。
とくに内部の語彙（役割の呼び名）は未決。前の実装の独自用語は持ち込まない。

## ドキュメントの使い分け

**仕様と検討ログを混ぜない。** 混ぜると、却下した案が仕様として読まれる。

| 文書 | 何を書くか | 更新の仕方 |
|---|---|---|
| `docs/specs/*.md` | **決まったこと**。アーキテクチャ決定もここ | **更新する**（追記ログにしない） |
| `docs/notes/*.md` | 検討の経緯・却下した案・訂正の履歴 | **追記する**（消さない） |
| `docs/requirements.md` | 要件（出所つき） | 要件が変わったときだけ |

- **決めたら、まず `docs/specs/` を更新する。** ノートに書いただけで済ませない
  ——次に読む人はノートを読まない
- **決めた理由と、却下した案は `docs/notes/` に残す。** 仕様書に書かない
  （仕様書が読めなくなる）。ノートは消さない——「なぜそうなったか」が失われると、
  同じ議論をやり直すことになる
- **仕様書に書くのは決まったことだけ。** 未決は「まだ決まっていないこと」の節に
  分けて置く。決まっていないものを、決まっているかのように書かない

## 守る規則

前の実装で実際に効いたものだけを持ち込んだ。増やすときは理由を1行書く。

- **1. 自己申告を信頼しない。** テスト・ビルドの結果は自分で実行して確かめる。
  **これは自分の推論にも掛かる**——測る前に犯人を決めない
- **2. エラーを握りつぶさない。** 回復できないなら止まる。黙って別の経路へ落ちない
- **3. 真実は一箇所。** 導出できる値を保存しない。写しを持つと、いつか食い違う
- **4. 観測は、観測される機構の外側に置く。** 中に置くと、機構が止まったとき観測も一緒に止まる
- **5. 任せてよいのは仕事であって、仕事が行われていることの確認ではない**
- **6. 間欠的に落ちる試験は、機構が壊れている合図。** 待ちを延ばさない、リトライを足さない。
  根拠は計測（何回中何回落ちるか）。直さない選択はあってよいが、そのときは記録を残す
- **7. スコープ外に触らない。**「ついで」の修正をしない
- **8. 仕様と実態が食い違ったら、黙ってどちらかに寄せない。** 記録を残して人に上げる
- **9. TypeScript strict。** `any` を書くならその行に理由を書く
- **10. 依存を足す前に、標準ライブラリと既存資産を見る。** 足すなら理由を1行
- **11. 用語は一般的なものを使う。** 製品名 `Banto` 以外に独自の呼び名を作らない。
  初見のエンジニアが機能を想像できる語を選ぶ。前の実装はここで失敗した
  （独自の日本語を内部用語に多用し、コードを読んでも何をするものか分からなくなった）
- **12. 機構を作る前に、それに名前があるか調べる。** 名前がある（すでに解かれている）なら、
  既知の答えを使って5分で通り過ぎる。名前が無いなら、そこが自分で考えるべきところ。
  前の実装はこの配分が逆で、解決済みの問題に設計の時間を使い果たした
  （`docs/lessons.md` の教訓18 に、これから当たるものの名前の一覧がある）

## 進め方

**3段で進める**（2026-08-30 決定）。**段を飛ばさない。**

| 段 | 何をするか | 何が終わったら次へ |
|---|---|---|
| **1. PoC** | **未決事項を潰すためだけのコードを書く** | 仕様書 §10 の「C. 設計が要る」が決まる |
| **2. モック** | **機能を網羅した画面**を作る。中身は繋がなくてよい | **UI が固まる**（§10 の「D. 作らないと決まらない」が決まる） |
| **3. 本実装** | Phase 0 から順に | `docs/requirements.md` の完了条件 |

**PoC のコードは捨てる前提で書く。** 目的は**決めること**であって、動くものを
残すことではない。**PoC が本実装に流れ込むと、前の実装と同じ失敗になる**
——形が決まる前に書いたものが、いつのまにか土台になる。

**モックを飛ばして実装しない。** 仕様書 §10 の D 群（Project 単位の Module 管理
UI・設定画面の配置・各 Module の面）は**画面を見ないと決まらない**と分類してある。
設計書の上で決めようとしない。

**いまはモック段。実装がどこまで進んだか、次に何をやるかは `mock/README.md`
にまとめてある——モックの作業を続けるときは、まずそこを読む。**

**どの未決をどう解くかは `docs/specs/v4-architecture.md` §10.0〜10.2**
——解決の仕方で4つに分け、依存の順序と Phase ごとの最短経路を書いてある。

### Phase と完了条件

`docs/requirements.md` の末尾。

**完了条件は「要件を満たす」ではなく「計測が実際に走り、数値を返す」。**
設計が正しいことと、実装で再現されることは別である。

**Phase 0/1 でモジュールを増やさない。** 契約は先に決め、中身は後。
