# imp-0019 Kobo を再開する前に片づける残り（キュー棚卸しの未了分・残骸ワークツリー）

- 起票: 2026-08-13（番頭）
- 更新: 2026-08-13（番頭。未マージ6本の処分と、Kobo の方針差し替えを反映）
- 由来: 枝「キューの棚卸し」(thread-75) / 枝「Kobo復旧（第0波）」(thread-73) を畳むにあたり、
  **枝の中に置き去りにできない宿題**をここへ移したもの
- 状態: open（**残るのは「判定が帳簿に無い6件」と、帳簿を畳めない道具の問題**）

## 前提（いまの凍結状態）

banto プロジェクトは **取り込み（watcher）もマージキューも停止**している
（PO 指示 2026-08-13T10:14。task-0100 のマージを含め自動マージを全停止、番頭が直接管理する体制へ）。

**2026-08-13 追記:** 受け持ち自体は**外さない**方針が枝 thread-77 で確定した。
外すと「直したものを実地で確かめる場」が無くなり、机上の作り直しになって同じ失敗を再生産するため。
ただし**自分を直している最中の機構に自分の改修を運ばせるのは危険**なので、弁は止めたままにし、
動作確認は loamium など別プロジェクトで行う。**追加の操作は不要**（今の状態がそのまま正解）。

## 済んでいること（2026-08-13 の棚卸しで確定）

帳簿に照合結果が commit 付きで残っている。**枝ではなく帳簿が記録**なので、枝を畳んでも失われない。

| task | 判定 | 根拠 |
|------|------|------|
| task-0102 | 中身は main に統合済み → 畳んだ | d845253 / マージ d9bb43b |
| task-0104 | 同上 | b78601a・22d94ec / マージ 10f01af・28859d2 |
| task-0106 | 同上 | b809cfa / マージ 8550dd0 |
| task-0109 | 同上 | 8bed271 / 710a92c |
| task-0112 および task-0113〜0146（34件） | inc-0063 の誤検知による重複起票の残骸 → 畳んだ | task-0097 を畳んだため不要 |
| task-0097 | 中身は手動コミット 86fb5a9 で main に統合済み → 畳んだ | inc-0063 |
| task-0093（検証プロファイル分け） | main に入っている | `meta/environments.yaml` に test / test-docker / dev の3プロファイルが実在 |
| inc-0053（docker ネットワーク leak） | 修正済み・closed | c8d7848 ＋ acceptance テスト追加 |

## 残っていること

### 1. 照合の判定が帳簿に残っていないタスク（8件 → 6件）

棚卸しは 09:17〜09:24 に走り、10:14 に PO が全停止した。**下の各件は状態が変わっておらず、
「照合した上で進めると判断した」のか「未着手のまま残った」のかが帳簿から読めない。**

- ~~`task-0100` 道具定義を人が書き直す（ADR-0019 決定84）~~ — **2026-08-13 に番頭が直接マージ（下記）**
- ~~`task-0101` コンフリクト解消: task-0090 vs main~~ — **task-0090 のマージで用済み（下記）**
- `task-0103` 決定96 の設定画面
- `task-0105` ADR-0019 実装後の実測
- `task-0107` 段1b: 採用（policy）を核の台帳の母集団へ
- `task-0108` 段2: 工房の tierAssignments を廃す
- `task-0110` spec-daemon-core §3.5 のモデル tier 表を ADR-0021 の形へ
- `task-0111` 番頭と職人を Claude で回したときのサブスク消費を測る

**やること:** 再開前に1件ずつ「中身が既に main にあるか」を照合し、判定を帳簿に書く
（済みなら畳む／生きているなら理由とともに残す）。判定を書かずに再開すると、
既に main にあるものが再実装され、スコープ重複ゲートで後続を止め、inc-0063 と同じ churn を生む。

**照合は「ブランチにコミットが載っているか」では足りない**（下の task-0091 がその実例）。
**中身が既に main に入っていないか**まで見ること。

### 2. 手が止まったままのタスク（4件）→ **片づいた（2026-08-13）**

PO の指示により、番頭が職人へ直接投げて処分した。**Kobo は通していない**（停止中のため）。
テストは docker の test プロファイルで機構が実測（終了コード0 / 1730件 / fail 0 / skip 5）。

| task | 処分 | 根拠 |
|------|------|------|
| task-0090 職人のツール結果にも退避＋ポインタ | **main へマージ** | e94c614 / マージ 9292f30。コンフリクトは pool.ts の import 1箇所のみ |
| task-0091 閉じた枝を開いてもすぐ閉じない | **不要（取り込むものが無い）** | main 側に同じバグの後発かつ上位の修正が既にある（useBantoSession.ts の遷移判定、App.tsx の重複除去、回帰テスト tests/trunk-branch.spec.ts:655）。当てると意図的に消した重複が復活する |
| task-0092 npm test が負荷で間欠的に落ちる | **main へマージ** | マージ 539bdb0。以降の全計測で間欠失敗ゼロ |
| task-0098 gate-review-fixes fix2-b のコンテナ決定的失敗 | **保留** | PO 判断（2026-08-13）: Kobo 本体の見直し対象。**realign 計画の第4便（環境定義の分裂）に統合する** |

あわせて task-0100（道具定義の書き直し）も同じ扱いでマージした（マージ 50ee9b6）。
`thread-tools.ts` のコンフリクト2箇所は「**機能は main を残し、文体は決定84 の書き直しを採用**」で統合。

**波及して見つかったもの:** task-0090 の退避は `extensionPaths` 経由＝**pi 職人の仕組み**に載っており、
Claude Agent SDK 経路の職人はこれを読まない。実運用の職人はほぼ全て Agent SDK 経路なので、
マージしただけでは task-0089 の事故（長いツール結果の直後に応答が返らない）は塞がっていない。
→ 2026-08-13、番頭が職人へ直接修正を委譲（起票せず直す。PO の方針）。
**realign 計画の第2便でも「pi 経路だけ直して塞いだつもりにならない」よう織り込む。**

### 3. 残骸ワークツリー → **片づいた（2026-08-13）**

- マージ済みワークツリー9つとブランチ9本を削除（`/home/ubuntu/worktrees` 16G → 12G）
- 未マージ6本のうち、マージした3本と不要と判明した2本を処分。task-0098 のみ保留のまま残す
- **消し切れなかった残骸**: `task-task-0089` / `task-task-0093` / `task-task-0095` の3ディレクトリ（計約47MB）。
  node_modules の一部が root 所有で、職人のシェルは `no new privileges` により sudo が通らない。
  git 上は孤児化済み（`git worktree list` に出ない）ので実害は無いが、消すには root 権限が要る。
- main は origin へ push 済み（2ff4d37 → 9292f30）

### 4. 「工場の外で決着した」を表せる終い方が無い（2026-08-13 判明）

**`kobo.abandon` は failed 状態のタスクにしか効かない。**
工場の外で片づいたタスク（paused / queued / review-ready のまま中身が main に入ったもの）を
帳簿の上で畳む手段が無く、上の表の判定を帳簿へ書き戻せなかった（この文書が代わりの記録になっている）。
枝 thread-77 でも独立に同じ壁に当たっており、旧レポートのズレ #6「詰まったものを番頭が降ろせる」の実物。

→ **realign 計画の第2便に実装項目として入る。**（第1便は既に職人へ委譲済みで、割り込ませると質が落ちるため）
この文書は、それが入るまでの申し送りとして活きる。

## 関連

- inc-0063 マージキューが空 rebase をコンフリクトと誤検知して周回した（34件の残骸の原因）。
  **正体が判明**: task-0097 の差分は実質 `Dockerfile.test` のコメントの巻き戻しで、
  main 側のコメントの方が上位互換（task-0093・0097 の両方を出典に挙げ根本原因まで書いてある）。
  解決するたび main の情報が失われる形の衝突だった
- inc-0064 検証環境の process ドライバが稼働中の本番ワークツリーで `npm ci` を走らせる（**別件・open**）
- **desk/reports/2026-08-13-kobo-vs-po-intent.md** — PO の期待像と実装の突き合わせ（1,204行）。
  **骨格は既に出来ており作り直しは不要**という結論。最大のズレは `review-policy.ts:33` の
  `DEFAULT_REVIEW_STAGE = "banto"`（既定で番頭の承認が必須）
- **desk/reports/2026-08-13-kobo-realign-plan.md** — 現行の実行計画（第1〜5便）。**これが有効**
- desk/reports/2026-08-13-kobo-rebuild-plan.md — 旧計画（裁定 D）。**差し替え済み・無効**
- desk/reports/2026-08-13-kobo-rethink.md — 旧レポート。**分析としては有効だが、処方（段階1〜5の作り直し）は差し替え済み**
