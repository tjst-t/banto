---
id: task-0250
type: task
kind: feat
title: リソース不足の断りメッセージと一覧表示を更新
status: done
scope:
  paths: ["packages/banto-worker-pool/src/pool.ts", "packages/banto-worker-pool/src/worker-tools.ts", "tests/acceptance/**"]
acceptance:
  - { id: a1, text: "リソース不足で断られたとき、理由（不足リソース量・想定消費・空き）が分かるメッセージになる", verify: "npm test" }
  - { id: a2, text: "worker.list の内訳表示に想定消費・空きリソースが載る", verify: "npm test" }
---

# 依頼

Worker Pool の並行制御を「想定消費リソース」ベースに変える設計（work/reports/2026-08-17-worker-resource-based-concurrency.md・accepted）のタスク D。

リソース不足による拒否を表現する文言（`declineHeadline` / `WORKER_LIMIT_CODE`）を拡張し、`worker.list` の内訳表示に想定消費・空きリソースを追加する。

設計書の「5. 既存の枠との整合・後方互換」を参照。acceptance テスト `worker-pool-audit-reservation.spec.ts`（a1〜a6）が枠表現を契約として固定しているため、文言変更はテスト修正を伴う（タスク E と調整）。

## 完了条件
上記 acceptance をすべて満たすこと。
