---
id: task-0063
type: task
kind: feature
title: 費用の上限を Kobo が持ち、超えるタスクは拒否する（決定67）
status: draft
parent: epic-0010
refs: [adr-0013, adr-0011]
scope:
  paths: ["packages/banto-daemon/**", "meta/**", "tests/acceptance/**"]
acceptance:
  - { id: a1, text: "同時実行数の上限が層B設定から読める（環境変数だけに依存しない）。既定は現在と同じ5" }
  - { id: a2, text: "許す最大 tier を設定でき、それを超える model_tier を持つタスクは**拒否**される。黙って下の tier へ丸めない（決定34f と同じ形）" }
  - { id: a3, text: "拒否の理由がイベントに残り、番頭から読める。『積んだのに動かない』が黙って起きない（I2）" }
  - { id: a4, text: "監査（reasoning 固定）が上限に掛かる場合の扱いが決まっていて、テストがある——上限で監査が回らなくなるなら、それは設定の誤りとして起動時に分かる" }
  - { id: a5, text: "Kobo はモデル名も金額も知らないまま（決定60a）。設定に現れるのは数と等級だけ" }
  - { id: a6, text: "npm run typecheck / npm test が通る", verify: "npm run typecheck && npm test" }
---

## 背景

ADR-0013 決定67（PO裁定 2026-08-06）。

**勝手に丸めない**のが要点。上限を超えたタスクを黙って `fast` へ落とすと、PO は「安く速く
終わった」と読み、実際には要求水準を満たしていない成果を受け取る——**拒否すれば、上限を
上げるか要求を下げるかを人が決められる**（決定34f と同じ考え方）。

`tier` は等級であってモデル名でも金額でもないので、これを Kobo が持っても決定60a とは
矛盾しない。**実費（トークン課金）の観測は Worker Pool 側の課題**として残す——金額を
知っているのはモデルを解決する側であり、Kobo に持たせると公開の口を1つ増やすことになる。

## 注意

- **監査は `reasoning` 固定**（spec-daemon-core §3.5）。上限を `standard` にすると監査が
  1本も回らなくなる。上限と監査の関係は起動時に分かるようにする（a4）
- 失敗駆動の昇格（監査不通過で一段上）も上限に掛かる。掛かったときに黙って据え置くのか
  拒否するのかを決めること

## スコープ外

- 実費（金額）の計測・予算（Worker Pool 側の課題）
- `po_required_paths`（同じ層B設定ファイルに同居するが、実装は Phase 2 の出口タスク）
