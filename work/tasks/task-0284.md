---
id: task-0284
type: task
kind: fix
title: settings 実装変更に追従していない acceptance テストを更新する
status: queued
written_by: kobo
refs: ["63cd3239", "6eb62825", "task-0282"]
scope:
  paths: ["tests/acceptance/chapter-model.spec.ts", "tests/acceptance/claude-agent-worker.spec.ts"]
acceptance:
  - id: a1
    text: chapter-model.spec.ts の全テストが通る（chapterModel を roles 統合表として検証）
    verify: BANTO_WORKER_POOL_URL=http://127.0.0.1:1/api/worker-pool BANTO_ENV_POOL_URL=http://127.0.0.1:1/api/environment-pool node --import tsx --test tests/acceptance/chapter-model.spec.ts
  - id: a2
    text: claude-agent-worker.spec.ts の全テストが通る（現在の settings 実装を検証）
    verify: BANTO_WORKER_POOL_URL=http://127.0.0.1:1/api/worker-pool BANTO_ENV_POOL_URL=http://127.0.0.1:1/api/environment-pool node --import tsx --test tests/acceptance/claude-agent-worker.spec.ts
  - id: a3
    text: npm test が通る（既存の不整合が解消される）
    verify: npm test
  - id: a4
    text: packages/ 配下のソースを変更していない（テストファイルのみ）
---
## 背景

main の settings 実装が次のコミットで変更されたが、acceptance テストが追従していない。

- 63cd3239 — pi.agent 接続情報区画を削除（llm に統合）し、worker-pool 区画を「アイドル安全弁のみ」に縮小（タイトル Worker Pool）。packages/banto-worker-pool/src/settings.ts が変わり、view は WorkerSettings.tsx が削除されて registry の別 view に置き換わった
- 6eb62825 — 「役割とモデル」統合表を3列テーブル化。createCoreSettingsSections の戻りは places / network / roles（役割とモデル・chapterModel・章要約は roles 内に統合）＋モジュール。独立した chapterModel 区画は無くなった

このため、古い区画構造（chapterModel 独立区画・WorkerSettings 区画）を期待する次の2テストが main 自身で落ちている（12本）。これは task-0282（チャットの Thinking 折りたたみ）など後続タスクのマージ前ゲートを巻き添えで落とす既存不整合。

- tests/acceptance/chapter-model.spec.ts — 3本落ち（例: sections.find((s) => s.id === "chapterModel") が undefined、「chapterModel 区画が無い」）
- tests/acceptance/claude-agent-worker.spec.ts — 9本落ち（例: view:"WorkerSettings" を期待するが実装は Worker Pool 区画のみ、「アイドルの安全弁は0以上の数で指定してください（受け取った値: undefined）」）

参考: tests/acceptance/settings.spec.ts は新構造（roles 統合）に合わせて更新済みで通っている。136行目のコメント「中核は places / network / roles（役割とモデル、chapterModel・章要約もこの中に統合）＋ モジュール」が新構造の定義。

## 目的

2つの acceptance テストを**現在の main 実装**に合わせて更新し、npm test を green にする。実装（packages/ 配下）は変更しない——テストが新実装を正しく検証する形にする。

## 完了条件

1. tests/acceptance/chapter-model.spec.ts が、chapterModel を「roles 統合表の中の章の要約の行」として検証する形に更新され、全テストが通る
2. tests/acceptance/claude-agent-worker.spec.ts が、現在の settings 実装（Worker Pool 区画・アイドル安全弁）を検証する形に更新され、全テストが通る
3. npm test（env 検証）が通る
4. packages/ 配下のソースは一切変更しない（テストファイルのみ）

## 注意

- 実装を「古い構造に戻す」方向の変更はしない。実装は main に確定済みのため、テストを新実装に合わせるのが正しい
- settings.spec.ts は既に新構造なので触らない
- テストの期待値を変えるときは、新実装の仕様（統合表の行 id・区画タイトル・保存キー）を実装から読み取って書くこと。推測で書かない
