---
id: task-0090
type: task
kind: fix
title: 職人のツール結果にも退避＋ポインタを（長いツール結果後の応答停止の対処）
status: queued
review: banto
scope:
  paths: ["packages/banto-worker-pool/src/**", "tests/acceptance/**"]
acceptance:
  - { id: a1, text: "職人のツール結果が、番頭と同じ「ファイルに退避してポインタだけを渡す」仕組みを通る（長い結果は直接モデルに入らない）", verify: "npm test" }
  - { id: a2, text: "長いツール結果を受け取っても、職人の応答生成が停止しない（agent_exited_without_report が起きない）", verify: "npm test" }
  - { id: a3, text: "退避した結果の読み出し（ポインタ経由）が職人からできる", verify: "npm test" }
---

# 依頼

## 何が起きたか（2026-08-10・deepseek 停止調査より）

職人（pi エージェント）が「長いツール結果（最大50KBの read 結果等）を受け取った後、応答生成が止まる」問題が発生した（task-0089 で2回連続 agent_exited_without_report）。

- 職人のツール結果は**加工されず直接モデルへ渡る**（banto は pi --mode rpc の起動と prompt 注入のみ。退避＋栞の ArtifactStore は番頭専用で、職人経路には無い）
- 番頭経路には「長いツール出力をファイルに退避して、ポインタ（栞）だけをモデルに渡す」仕組みがある（PO の記憶: 「長いツール結果は直接モデルに入れずに、ファイルに置いたうえでそこへのポインタだけを渡した」）
- 職人経路にも同じ退避＋ポインタの仕組みを適用すれば、長いツール結果が直接モデルに入らず、応答停止（コンテキスト溢れ・モデル側の空応答）を防げる

## 修正内容

職人のツール結果にも、番頭と同じ退避（ファイル保存）＋ポインタ（栞）の仕組みを適用する。

- 挿入点の候補: pi extension の tool_result イベント（--extension 経路は既存）
- 長い結果はファイルに保存し、モデルには「ファイルの場所・栞」だけを渡す
- 職人がその結果を読む必要があるときは、ポインタ経由で読み出せるようにする

## 完了条件

上記 acceptance をすべて満たすこと。

## 補足

- レビューは po（PO の判断が要る）: このタスクは deepseek 停止調査の枝（thread-63）で PO 直行とされた
- この定義は 2026-08-10 に一度失われた（番頭の file.write による上書き）ため、Kobo の帳簿情報（タイトル・スコープ・レビュー）と deepseek 停止調査の文脈から**復元**した。元の本文・受け入れ基準と完全に一致しているかは未確認
