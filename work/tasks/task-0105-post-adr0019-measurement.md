---
id: task-0105
type: task
kind: improvement
title: "ADR-0019 実装後の実測（呼び出し率と委譲への回り込み）"
status: queued
refs: ["adr-0020", "adr-0019"]
scope:
  paths: ["packages/**", "docs/adr/**", "work/**"]
acceptance:
  - { id: a1, text: "npm test / npm run typecheck が通る" }
  - { id: a2, text: "稼働中の banto に反映し、起動ログで確認している" }
review:
  policy: manual
---
## 背景

ADR-0019 の「実測で確かめること（実装後）」2項目が未実施。**実機はいま提示56本で動いており、
測れる状態にある**。

## やること

1. **仕分け後の呼び出し率**。位置の実測 0/10 → 16/16 が、数を減らすことでも再現するか。
   ADR-0019 の切り分け（条件 A〜F）の装置を使う
2. **番頭が「持っていない能力」に当たったとき、委譲へ回れているか**。遅延開示を捨てた以上
   ここが唯一の逃げ道。実測では `bash` 23回・`web.fetch/search` 14回の空振りがある
3. **撤退条件を数字で決める**：委譲へ回れた率が X% を下回ったら決定B（遅延開示をしない）を再考する

**反省を繰り返さない**：n を十分に取る／題材を自分で選ばない／合成データで「直った」と言わない／
**モデルは実際に使うもので測る**。
