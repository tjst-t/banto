---
id: task-0164
type: task
kind: fix
title: 記録の書き戻しが縮むのを拒み、章畳みの後も記録が残ることを機械で固定する
status: queued
written_by: kobo
environment: test
governance: true
model_tier: reasoning
scope:
  paths: ["packages/banto-host/src/thread-store.ts", "packages/banto-host/src/threads.ts", "tests/acceptance/**"]
acceptance:
  - id: a1
    text: ThreadStore.replace() が、そのプロセスが把握している行数より少ない entries での書き戻しを拒む。拒んだときは黙らず、拒んだ内容を別名（例 <threadId>.jsonl.rejected-<ISO8601>）へ退避し、console.error で「何本の記録が何本に縮もうとしたか」を出す。基準は実ファイルの行数ではなく、このプロセスがメモリに載せた行数（読み込み時に壊れた行を飛ばした場合でも誤検知しないこと）
    verify: npm test
  - id: a2
    text: 章畳みが成功したあとに積まれた記録が、ディスクへ書き戻されることを固定する試験がある。ThreadRegistry + ThreadStore を一時ディレクトリで組み立て、ChapterKeeper に偽の summarize を渡して closeChapter() を成功させ、flush の後に別インスタンスの ThreadStore で読み直して、(1) role:"chapter" のエントリと (2) 章畳みの後に record した発言の両方が読めることを確かめる
    verify: npm test
  - id: a3
    text: 縮む書き戻しが拒まれることの回帰試験がある。意図的に短い entries 配列で replace() を呼び、ディスクの内容が保たれ、退避ファイルが作られ、ログが出ることを確かめる
    verify: npm test
  - id: a4
    text: 既存のテストが全部通る（挙動を壊していない）
    verify: npm test
  - id: a5
    text: 行数だけでなく、**末尾が食い違う書き戻しも拒む**。ディスクの記録にある行が、書き戻しに来た記録に含まれていない（前方一致でない）ときは、行数が同じか多くても上書きせず、a1 と同じように退避して知らせる。今回の事故は「古いメモリで 33 時間ぶんを上書きし続けた」もので、この検査1つで事故は発生時点で止まっていた。回帰試験も置くこと
    verify: npm test
  - id: a6
    text: **要約器が連続で失敗したあとでも、章を畳めたら role:"chapter" の印が記録に入る**ことを試験で固定する。実機では要約器が2回続けて失敗した直後から壊れ、**その後 21 回章を畳めたのに印が1つも記録に入っていなかった**（誰も気づかなかった）。偽の summarize を**最初の2回だけ投げる**形にし、3回目の成功で印がディスクに残ることを確かめる
    verify: npm test
---
## 先に読むこと（1）：**マージで衝突が出たら、こう並べる**

`packages/banto-host/src/thread-store.ts` の `replace()` を、**task-0161 とこのタスクが同じ場所で触る**。

- **task-0161** … 書き込みの1行を `writeFileAtomicSync`（tmp + fsync + rename）に**替える**
- **task-0164（これ）** … **その手前にガードの呼び出しを足す**

**したがって `task-0161` → `task-0164` の順にマージすると、`replace()` 内で小さな衝突が出る。**

> **解き方：「①ガード（縮小・前方一致の検査） → ②原子的な書き込み」の順に並べるだけでよい。**
> **互いの意図は潰し合わない。** ガードは「書いてよいか」を決め、原子的書き込みは「どう書くか」を決めている。
> **どちらか一方を捨てないこと。** ガードを消せば inc-0075 の事故が素通りし、原子的書き込みを消せば
> 書き込み途中の千切れが戻る。

**この順番を守れば、他に調整は要らない。**

## 先に読むこと（2）：**task-0161 は、着手の時点で main に入っていなかった**

以前この本文には「着手する時点の `main` に task-0161 が入っているはず」と書いてあったが、**外れた**。
`thread-store.ts` は着手時点でも `fs.writeFileSync` の直書きのままで、原子的書き込みは
`task/task-0161` ブランチ上（未マージ）にあった。

**職人は現行 main の上に実装した。**スコープ外のファイル（`banto-core/src/atomic-write.ts` 等）を
持ち込まないための判断で、**これは正しい**。上の「先に読むこと（1）」は、その結果として必要になる。

## 先に読むこと（3）：**検証コマンドは `npm test`**

このリポジトリの試験は **`node:test`**（`npm test` / `npm run test:one`）で走る。**vitest は依存に無い。**
受け入れ条件に `npx vitest run` と書いてあったのは**契約の誤り**で、実装した職人が指摘して返してきた。
**訂正済み**（全条 `npm test`）。

---

## 背景（何が起きたか）

2026-08-16、幹 thread-61（「banto開発」）の**画面用の記録**から、**8/14 15:22 〜 8/16 00:48 の約33時間ぶんが失われた**。PO の画面には8/14夕方以降が一切出なくなった。

banto は2つの別々のものを保存している（`thread-store.ts` の冒頭コメント参照）。

1. **POに見えていた会話** … `<BANTO_DATA_DIR>/threads/thread-NN.jsonl`
2. **番頭の文脈** … Claude Agent SDK のセッションファイル

失われたのは **1 だけ**で、2 は無傷だった。冒頭コメントが「2 だけだと番頭は覚えているのに画面が空になる」と警告している、まさにその状態が起きた。

**被害を「1行」ではなく「33時間」にしたのは、書き戻しが全文上書きだからである。**

- `ThreadRegistry.persist()` → `flush()` → `ThreadStore.replace()`
- `replace()` は**無条件の全文上書き**。書く前に「行数が減っていないか」を検査しない
- **メモリが真実で、ファイルはその写し**。だからメモリに無いものは、次の書き戻しでファイルからも消える

## 原因（未確定。このタスクの目的ではない）

「なぜ 8/14 15:22 起動のプロセスだけ書き戻しが止まったのか」は**特定できていない**。調査で潰した候補は次のとおり（詳細は `work/inbox/incident/inc-0075-trunk-transcript-lost-33h.md`）。

**反証済みの候補**
- ディスク逼迫による ENOSPC 等 … 全期間のログに ENOSPC / EACCES / EIO は1件も無い
- `persist()` のデバウンスが永久に詰まる … `flushAll` / `flush` は `pendingSaves` を詰まりの判定に使わず、引いて消して**無条件に書く**
- `timer.unref()` で発火しない … libuv の `uv__next_timeout()` はタイマーヒープの最小値をそのまま返し ref 状態を見ない
- `startChapter()` で harness の購読が切れる … `startChapter()` は `this.listeners` をクリアしない
- 「thread-61 だけモデル解決に失敗して特別な経路」 … あの警告はほぼ全スレッドで再起動のたびに出る定型
- `this.threads.set(` の二重登録 … 呼び出しは2箇所だけ。索引の重複も0件
- 「毒の行を毎回読み直していた」 … gen0 の凍った1754行が現行ファイルの1〜1754行と**バイト完全一致**
- `flushAll()` の中断 … 8/16 00:19:29 の書き込み跡で、thread-61（37番目/106本）より**後ろが全部書けている**

**残っている矛盾（未解明の核心）**
- ch-0007〜0027 の**21本は普通に畳めている**のに、`role:"chapter"` の印が記録に1つも入っていない
- **断絶期間の内側で正常終了が15回**あり、`flushAll()` は走っていた。それでも書かれなかった
- `replace()` はメモリ全件の全文上書きなので、**メモリに在れば必ず出てくる。出てこなかった＝メモリに無かった**

**だからこのタスクは「原因を直す」ものではない。原因が何であれ効く安全弁を入れ、核心の疑問を試験で固定するものである。**

## やること

### 1. `ThreadStore.replace()` に「縮む書き戻し」を拒む番人を入れる

`Thread.transcript` は `record()` の性質上、**要素数が減ることはない**（末尾の連結・実行中の行の更新はあっても削除は無い）。この不変条件を書き手側が検査していないのが穴である。

- `ThreadStore` が threadId ごとに「**このプロセスが最後に書いた／読み込んだ行数**」をメモリに持つ
- `replace(threadId, entries)` で `entries.length` がその値より小さければ、**書き込みを拒む**
- **黙って拒まないこと（I2）**。拒んだ内容を `<threadId>.jsonl.rejected-<ISO8601>` のような別名で退避し、`console.error` で「何本が何本に縮もうとしたか」を出す
- **基準は「実ファイルの行数」ではなく「このプロセスがメモリに載せた行数」にすること。** 起動時に壊れた行を飛ばして読んだ場合、実ファイルより1行少ないのは正常なので、実ファイル基準だと誤検知する

### 2. 章畳みの後も記録が残ることを、試験で固定する

新しい受け入れ試験（例 `tests/acceptance/chapter-close-persists.spec.ts`）を置く。**LLM 無しで機械的に再現できる形にすること。**

既存の2つの型を合成するのが自然：
- `tests/acceptance/thread-persistence.spec.ts` … `ThreadRegistry` + `ThreadStore` + `FakeSession` で再起動を跨いだ永続化を検証
- `tests/acceptance/chapters.spec.ts` … `ChapterKeeper` に偽の `summarize` を渡して章畳みの機構だけを検証

骨格：
1. `thread.record({role:"po",...})` / `{role:"banto",...}` を数件積む
2. `ChapterKeeper` を組み立て、`onChapterClosed` に `server.markChapter` 相当（＝`thread.record({role:"chapter",...})` を呼ぶ関数）を渡す
3. `await keeper.closeChapter()` を成功させる（偽の `summarize` は即解決）
4. **核心のアサーション**：`flush` した直後、**別インスタンスの** `new ThreadStore(dir).transcript(id)` でディスクから読み直し、`role:"chapter"` のエントリが読めること
5. さらに章畳みの**後**に `record()` を1件積み、**再度ディスクから読み直して**それも読めること

### 3. 縮む書き戻しの回帰試験

意図的に短い `entries` で `replace()` を呼び、ディスクの内容が保たれること・退避ファイルができること・ログが出ることを確かめる。

## やらないこと（範囲外）

- **原因そのものの特定・修正**（未確定）
- **記録の原子的な書き込み**（task-0161 の持ち物。**先に読むこと（1）(2) を見よ**）
- **壊れた行を捨てずに退避する**（task-0163 / 0167 の持ち物。**こちらでは触らない**）
- 「記録があったのに一定時間書き戻されていない会話を検出して知らせる」仕掛け（task-0172）
- 未捕捉例外ハンドラの追加（別便。**握りつぶす形にはしないこと**という方針だけ記録しておく）
- `append` だけに寄せる設計変更（ストリーミング中のテキスト連結と `tool_start`→`tool_end` の同一行更新が追記だけでは表現できず、影響範囲が大きい。**まず縮小拒否ガードを入れるのが筋**）

## 気をつけること

- ここは**番頭核の永続化**であり、壊れると会話が消える。**既存の挙動を変えないこと**——正常な書き戻し（行数が同じか増える）は今までどおり通ること
- `settleInterrupted()`（`threads.ts`）は `transcript.map()` で置き換えるので**件数は変わらない**。`repairTrunkCards()` は**増やす**。どちらもガードに当たらないことを確かめること
- 拒否のログは journal で追えるよう `[banto]` 接頭辞を付けること
