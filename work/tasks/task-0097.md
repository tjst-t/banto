---
id: task-0097
type: task
kind: fix
title: Dockerfile.test の Node 22 → 24 更新
status: queued
scope:
  paths: ["docker/Dockerfile.test"]
acceptance:
  - { id: a1, text: "Dockerfile.test の base image が node:24-alpine（または同等の Node 24 系）に更新されている", verify: "cat docker/Dockerfile.test" }
  - { id: a2, text: "npm run typecheck が 0件エラーで通る", verify: "npm run typecheck" }
  - { id: a3, text: "npm test（docker 系7本除外後）が通る", verify: "npm test" }
  - { id: a4, text: "npm run test:docker（ホストの docker 環境下）が通る", verify: "npm run test:docker" }
  - { id: a5, text: "docker/test.yaml コンテナ内で npm test を実行したとき、Node 22 時の cancelled 54件が解消している", verify: "docker compose -f docker/test.yaml run --rm -v <repo>:<repo>:ro test npm test" }
---

# 依頼

## 何が起きたか

task-0093 の調査で、検証コンテナ（`docker/test.yaml`）内での `npm test` が
**Node 22 と 24 の差**によって 54件 cancelled になっていることが判明した。

- コンテナ: `node:22-alpine`（npm 10.9.8）→ 54件 cancelled
- ホスト: Node v24.18.1 → 全件 pass
- 確認済み: `node:24-alpine` にすると 59/59 pass

## やること

1. `docker/Dockerfile.test` の base image を Node 24 系に更新する
2. 必要に応じて、依存関係やビルド手順を Node 24 に合わせて調整する
3. 更新後、typecheck / test / test:docker が通ることを確認する
4. 検証コンテナ内でも cancelled が解消することを確認する

## 注意

- スコープは `docker/Dockerfile.test` のみ。他のファイルは触らないこと
- `package.json` の `engines` フィールドがあれば、そちらも合わせて確認すること
- 更新後のイメージサイズやビルド時間が著しく悪化しないこと
