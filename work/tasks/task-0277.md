---
id: task-0277
type: task
kind: fix
title: kobo.list に終端タスクを全件返す道（offset ページング）を足す
status: queued
written_by: kobo
governance: true
scope:
  paths: ["packages/banto-daemon/src/**", "packages/banto-host/src/**", "tests/acceptance/kobo-inbox-sweep.spec.ts"]
acceptance:
  - id: a1
    text: 起動時スイープが kobo.list の100件切り詰めを迂回して全終端タスクを取得できる（閉じたタスクが100件を超えても全件列挙できる）
    verify: npm run test:one -- tests/acceptance/kobo-inbox-sweep.spec.ts
  - id: a2
    text: 閉じたタスクが100件を超えていても紐づく未解決取次が stale として畳まれる
    verify: npm run test:one -- tests/acceptance/kobo-inbox-sweep.spec.ts
  - id: a3
    text: 全量と型検査が通る
    verify: npm test && npm run typecheck
---
【不具合】起動時残存取次スイープ sweepStaleInboxForTerminalTasks（packages/banto-host/src/kobo-po-decision.ts:306）は bin.ts の sweepStaleOnStartup で invoke('kobo.list',{state}) を呼ぶが、kobo.list（packages/banto-daemon/src/kobo-tools.ts）が MAX_ROWS=100 で limit をクランプし（行61・235）matched.slice(0,limit)（行~270）で100件目以降の終端タスクが一覧に載らない。closed が159件と100を超えており task-0270（index154）の取次 in-961f7dd2 が未解決のまま残った（実測）。task-0271 の in-225be960 は100件内で stale:superseded により掃かれ済み。task-0276 の起動時スイープ自体は正常動作。【直し方】kobo.list の parameters に offset を追加し matched.slice(offset, offset+limit) でページング可能にする。起動時スイープ側（kobo-po-decision.ts の listTerminalTasks 呼び出し）から offset を進めて全終端タスクを全件取得する。total は matched.length のまま。通常の kobo.list 表示（既定先頭100件・注記）は変えない。完了条件はスイープが全終端タスクの projectTag/taskId/status を得て resolveStaleInboxForTask で未解決取次を畳めること。
