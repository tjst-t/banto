---
id: task-0091
type: task
kind: fix
title: 閉じた枝を開いてもすぐ閉じない（App.tsx の useEffect 修正）
status: queued
review: banto
scope:
  paths: ["packages/banto-web/src/**", "tests/trunk-branch.spec.ts"]
acceptance:
  - { id: a1, text: "閉じた枝を開くと、開いたまま表示される（チャット欄なし・結論表示のまま閉じない）", verify: "npm run typecheck:web && npm test" }
  - { id: a2, text: "閉じた枝の「開き直して続ける」で枝を再開（reopen）できる", verify: "npm run typecheck:web && npm test" }
  - { id: a3, text: "見ている最中に枝が畳まれたときだけ、親の幹へ戻す効果が働く（畳んだ枝を開いたときは戻らない）", verify: "npm run typecheck:web && npm test" }
  - { id: a4, text: "回帰試験（閉じた枝を開いても戻らない・開き直して再開できる）が tests/trunk-branch.spec.ts に追加され、通る", verify: "npm run typecheck:web && npm test" }
---

# 依頼

## 何が起きたか（2026-08-10・PO 報告を opus 職人が調査）

PO 報告: **閉じた枝を開くと、すぐクローズされる**。チャットはできなくていいので、開いたままにしてほしい。あと閉じた枝もチャット再開できる仕組みがあるといい。

opus 職人による調査で、原因は以下の1箇所と特定された:

### ④ 閉じた枝を開くとすぐ閉じる（UI の効果の副作用）

`packages/banto-web/src/App.tsx:235-239`

```ts
useEffect(() => {
  if (branch?.state !== "closed") return;
  const parent = branch.parentId ? session.threadOf(branch.parentId) : undefined;
  if (parent) openThreadRef.current?.(parent.threadId);
}, [branch, session]);
```

コメント（App.tsx:229-234）のとおり「**見ていた枝が畳まれたら幹へ戻す**」（PO報告 2026-08-10）の実装。しかし判定が「**いま見ている枝が closed か**」だけなので、**「畳まれた瞬間」と「畳んだ枝をこちらから開いた」を区別できず**、PO が閉じた枝を開くと即座に親の幹へ戻す＝「開いたのにすぐ閉じる」ように見える。

閉じた枝への入口は2つあり、どちらもこの効果で跳ね返される:
- 幹に残る枝の札 `BranchCard`（Branch.tsx:59-62。状態は「畳みました」と出る＝押せる想定）
- 還った1行の「中身をひらく」（Branch.tsx:119-124）

### ⑤ 閉じた枝のチャット再開（機構は既にある・④が潰しているだけ）

`Room.tsx:203-208` に「**開き直して続ける**」（`thread_reopen`）の口がある。しかし `thread_reopen` を発火すると `thread_state` 配布が来て、配布された会話一覧で「見ている会話が open でない」判定が働き、既定（帳場）へ追い出される。`useBantoSession.ts` も同じ穴がある。

## 修正方針

1. **App.tsx**: 「**見ている最中に**枝が畳まれた」という**遷移**を判定する。直前に見ていた枝とその開き閉じを ref で持ち、「同じ枝が開いていた状態から閉じた」ときだけ親へ戻す。畳んだ枝をこちらから開いたときは戻さない。
2. **useBantoSession.ts**: `thread_state` 配布でも、閉じているだけで追い出さない。「繋がっていない間に畳まれた」＝遷移を見る。
3. **親の幹へ戻す**（開いている先頭＝帳場ではなく）。畳んだ枝の親にはまだ続きがある。

## 完了条件

上記 acceptance をすべて満たすこと。

## 参考

- task-0091 前回セッションの実装: コミット baba5eb、task/task-0091 ブランチ上
- 関連: `packages/banto-web/src/App.tsx`、`packages/banto-web/src/useBantoSession.ts`、`packages/banto-web/src/Room.tsx`

## 追加指示（2026-08-11・PO 判断）

回帰試験（a4）は**後で別タスク**で追加する（PO判断）。今回は実装のみで通す。
