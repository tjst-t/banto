---
id: task-0238
type: task
kind: fix
title: PO 必須の面が空であることと、空でも po を名乗れることを機械で守る
status: queued
written_by: kobo
scope:
  paths: ["tests/acceptance/review-default-policy.spec.ts"]
acceptance:
  - id: a1
    text: tests/acceptance/review-default-policy.spec.ts に、実リポジトリの meta/config.yaml を loadProjectConfig で読み review.poRequiredPaths が空であることを確かめる試験があり、なぜそれを固定するのか（PO の恆久指示・戻すときはこの試験も直す）がコメントに書いてある
    verify: npm run test:one -- tests/acceptance/review-default-policy.spec.ts
  - id: a2
    text: 同ファイルに純関数の試験が3つ足されている：(1) 一覧が空なら scope に packages/banto-web/src/views/ui.tsx を含んでも宣言 banto がそのまま段になる (2) 一覧が空でも宣言 po は po のまま (3) 一覧が空でも governance: true は po
    verify: npm run test:one -- tests/acceptance/review-default-policy.spec.ts
  - id: a3
    text: 既存の試験は一つも消えておらず、変更はこの spec ファイル1本に限られている（meta/config.yaml も review-policy.ts も触っていない）。全試験と型検査が通る
    verify: npm test && npm run typecheck
---
## 背景（前提を全部ここに書く。あなたはこの案件の記憶を持っていない）

banto のレビューは3段（ADR-0013 決定57）。段を決めるのは `packages/banto-daemon/src/review-policy.ts` の `resolveReviewStage`（301行目付近）で、判定は上から順に

1. `governance: true` なら `po`
2. 層B設定 `meta/config.yaml` の `review.po_required_paths` に `scope.paths` が触れるなら `po`（**タスクの宣言より手前で勝つ**）
3. タスクの宣言（`auto` / `po` / `banto` / 旧称 `manual`→`banto`）
4. 層Bの `review.default_policy`、無ければ `DEFAULT_REVIEW_STAGE`（＝`auto`）

2026-08-16、PO の恒久指示

> 私が明示的にお願いするとき以外、banto の開発でもユーザのレビューは省略してくれないかな。あんまり意味ないし。

を受けて、**task-0183 が banto 自身の `meta/config.yaml` の `po_required_paths` を空にした**（仕組みは残し、一覧だけを空にした。経緯は ADR-0013 決定66 の 2026-08-16 追記にある）。
これは**判定2 を効かせない**という統治の変更であり、**いまは設定ファイル1本の状態でしか担保されていない。**

**現物の確認（2026-08-16 夜・番頭が実測）**：`meta/config.yaml:37` は `po_required_paths: []`。**先行の task-0183 は着地済み**である。

## やること

`tests/acceptance/review-default-policy.spec.ts` に試験を足す（**既存の試験は消さない・書き換えない**）。
`resolveReviewStage` は task と config だけを見る純関数なので工場は立てない——既存のヘルパ（`config()` / `task()` / `repoWithConfig()`）をそのまま使うこと。

1. **実リポジトリの設定を読む試験**：リポジトリルートの `meta/config.yaml` を `loadProjectConfig` で読み、`review.poRequiredPaths` が空であることを確かめる。
   コメントに「2026-08-16 の PO の恒久指示を機械で守る。パスを戻すときはこの試験も一緒に直すこと——それが指示を上書きする意思表示になる」という趣旨を書く。
   リポジトリルートの求め方は同ファイル・近隣の spec の既存のやり方に合わせ、**新しい依存は足さない**。
2. **一覧が空でも宣言が効くこと**（純関数）：`poRequiredPaths` が空のとき、`scope.paths` に `packages/banto-web/src/views/ui.tsx` を含むタスクでも、宣言 `banto` がそのまま段になる（`po` に上がらない）。
3. **PO が明示的に頼む道が残っていること**（純関数）：`poRequiredPaths` が空でも、宣言 `po` は `po` のまま。
4. **統治コードは従来どおり**（純関数）：`poRequiredPaths` が空でも、`governance: true` は `po`。

## 進め方の注意

- **まず main を取り込んでから作業を始めること**（task-0183 の設定変更が入っていないと 1 が落ちる）。
- 触ってよいのは **`tests/acceptance/review-default-policy.spec.ts` だけ**。`meta/config.yaml` や `packages/banto-daemon/src/review-policy.ts` は**変えない**
  （触るとマージ前ゲートのスコープ検査で落ちる）。
- **試験を通すために製品コードを変えたくなったら、それは変えるべきではない兆候**である。そのときは報告に「何が食い違っているか」を書いて止まること。

## 走らせ方（2026-08-16 現在の実態。古い文面を写さないこと）

- **受け入れ基準に添えた検証コマンドは、Kobo が検証環境（あなたの外）で回します。**
  自分で**一式**（`npm ci` / `npm test` / `npm run typecheck`）を叩かないこと——あなたは 2 GiB のメモリ上限の中にいて、
  一式を回すと上限に当たって**あなたごと**強制終了されます（2026-08-16 に実測で7回）。
- **自分で叩いてよいのは軽い確認だけ**：1本の spec（`npm run test:one -- tests/acceptance/review-default-policy.spec.ts`）、
  1ファイルの型検査、`grep` / `git diff` などの読み取り。**この仕事は spec 1本なので、それで足りる。**
- `report_done` を呼ぶと、Kobo が監査へ回す前に受け入れ基準の検証コマンドを**検証環境の中で**回し、
  落ちていれば落ちた基準・終了コード・ログを**あなたへ差し戻します**（I1）。
