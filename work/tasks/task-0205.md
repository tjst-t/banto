---
id: task-0205
type: task
kind: fix
title: 同じ失敗の知らせを何度も配らない（配達済みの出来事を再送しない・判断待ちの催促を抑える）
status: queued
written_by: kobo
refs: ["inc-0063", "inc-0069"]
scope:
  paths: ["packages/banto-daemon/src/daemon.ts", "tests/acceptance/kobo-notice-dedupe.spec.ts"]
acceptance:
  - id: a1
    text: 同じ event id の出来事は、一度配達したら二度と配達されない
    verify: BANTO_WORKER_POOL_URL=http://127.0.0.1:1/api/worker-pool BANTO_ENV_POOL_URL=http://127.0.0.1:1/api/environment-pool node --import tsx --test tests/acceptance/kobo-notice-dedupe.spec.ts
  - id: a2
    text: 配達済みの印は再起動をまたいでも残り、起こし直した直後に古い出来事がまとめて再送されない
    verify: BANTO_WORKER_POOL_URL=http://127.0.0.1:1/api/worker-pool BANTO_ENV_POOL_URL=http://127.0.0.1:1/api/environment-pool node --import tsx --test tests/acceptance/kobo-notice-dedupe.spec.ts
  - id: a3
    text: 新しい出来事は必ず配達される（抑制が新着を食わない）
    verify: BANTO_WORKER_POOL_URL=http://127.0.0.1:1/api/worker-pool BANTO_ENV_POOL_URL=http://127.0.0.1:1/api/environment-pool node --import tsx --test tests/acceptance/kobo-notice-dedupe.spec.ts
  - id: a4
    text: PO 判断待ちの札が立っている間、同じタスクの同じ種類の催促の知らせが繰り返し配られない
    verify: BANTO_WORKER_POOL_URL=http://127.0.0.1:1/api/worker-pool BANTO_ENV_POOL_URL=http://127.0.0.1:1/api/environment-pool node --import tsx --test tests/acceptance/kobo-notice-dedupe.spec.ts
  - id: a5
    text: 番頭が畳んだ職人の報告が、取り下げ札として再送されない
    verify: BANTO_WORKER_POOL_URL=http://127.0.0.1:1/api/worker-pool BANTO_ENV_POOL_URL=http://127.0.0.1:1/api/environment-pool node --import tsx --test tests/acceptance/kobo-notice-dedupe.spec.ts
  - id: a6
    text: 抑制して配らなかったことが帳簿から読める（黙って落とさない）
    verify: BANTO_WORKER_POOL_URL=http://127.0.0.1:1/api/worker-pool BANTO_ENV_POOL_URL=http://127.0.0.1:1/api/environment-pool node --import tsx --test tests/acceptance/kobo-notice-dedupe.spec.ts
---
## 何が起きているか（別プロジェクトの実測・2026-08-16）

dentaku の task-0042 で、**同じ失敗の知らせが番頭へ繰り返し配られました。**

- **Kobo の帳簿の最後の出来事は `2026-08-16T08:40:39Z` の `rework session spawn failed: ... read ECONNRESET` 1件だけ**で、
  それ以降タスクは**一切動いていません**（`kobo.task` で確認済み）。
- **にもかかわらず、番頭のターンが同じ内容で3回起きました**：
  1. 「起きたこと: agent_exited_without_report」（**08:34 の分の再送**）
  2. 「【取り下げ】probe-worker-pool からの報告」（**既に読んで畳んだ職人の報告**が、取り下げ札として届いた）
  3. 「起きたこと: rework session spawn failed ... ECONNRESET」（**08:40:39 の分の再送**）
- そのたびに番頭は `kobo.task` を引いて「**新しい事故ではない**」と確かめる必要がありました。
- **しかもそのタスクは、既に PO 判断待ちの札（in-248b13b7）が立っていて先へ進めません。**
  **進めない件について、判断を促す知らせだけが増える**という形になっています。

**inc-0063 の「マージキューが同じタスクを積み続ける」と似た周回**です。

## 直す方針

1. **一度配った出来事を再配達しない。**同じ event id は**既配達として落とす**こと
   （配達済みの印をどこに持つかは実装者が決めてよい。**再起動をまたいでも効くこと**）。
2. **既に PO 判断待ちの札が立っているタスクについては、催促の知らせを抑える。**
   **同じ札が立っている間、同じタスクの同じ種類の知らせを繰り返し配らないこと。**
3. **ただし「新しい出来事」は必ず配ること。** 抑制は**同じ出来事の繰り返し**に対してだけ効かせること。
   **黙って落とすのがいちばん危ない**——落としたことが帳簿から読めること。

## 気をつけること

- **知らせを配らない方向へ倒しすぎないこと。**過去に「知らせが消えた」事故があります（inc-0069）。
  **落とすのは「同じ出来事の2回目以降」だけ**で、**新しい出来事・状態が変わった知らせは必ず届くこと。**
- **番頭が畳んだ職人の報告が「取り下げ札」として再度届く**経路も同じ穴の一部です。**畳んだ職人の報告は再送しないこと。**
- **`/var/lib/banto` 以下の実データに触らないこと。** 試験は必ず一時ディレクトリで。

## 走らせ方（**あなた自身が OOM で死なないために**）

あなたは 2 GiB の cgroup の中にいます。**`npm test`（約400秒・2400件）や `npm run typecheck` を袋の中で回すと自分が殺されます。**
確かめるときは**1本だけ**：

```
BANTO_WORKER_POOL_URL=http://127.0.0.1:1/api/worker-pool \
BANTO_ENV_POOL_URL=http://127.0.0.1:1/api/environment-pool \
node --import tsx --test tests/acceptance/kobo-notice-dedupe.spec.ts
```

全量の検証は依頼元が袋の外から回します。**1本だけ通したなら「1本だけ通した」と正直に報告すること。**
