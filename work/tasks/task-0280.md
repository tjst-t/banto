---
id: task-0280
type: task
kind: improvement
title: 未コミットの仕事記録を整理してコミットする
status: queued
written_by: kobo
scope:
  paths: ["packages/banto-daemon/skills/kobo-enqueue/SKILL.md", "docs/proposals/2026-08-18-thread-watchdog-and-auto-fold.md", "work/tasks/task-0277.md", "work/tasks/task-0278.md", "work/tasks/task-0279.md"]
acceptance:
  - id: a1
    text: 対象5ファイルが git にコミットされ、作業ツリー（main）に未コミットの変更が残らない
    verify: git status --porcelain | grep -q . && exit 1 || exit 0
  - id: a2
    text: 型検査と全量テストが通る
    verify: npm run typecheck && npm test
---
## 背景
main の作業ツリー（/home/ubuntu/ghq/github.com/tjst-t/banto）に、未コミットの仕事記録が残っている。これらは以前の作業で作られた・修正された記録文書で、内容は確定している。コードには触れず、これらを git にコミットして作業ツリーをクリーンにする。工場（Kobo）の流れ（ゲート→職人→監査→レビュー→マージ）の動作確認も兼ねる。

## 対象ファイル（これらだけを git add してコミットする）
- packages/banto-daemon/skills/kobo-enqueue/SKILL.md（既存の変更。内容は書き換えない）
- docs/proposals/2026-08-18-thread-watchdog-and-auto-fold.md（新規。作成済みなのでそのまま add）
- work/tasks/task-0277.md（新規。作成済み）
- work/tasks/task-0278.md（新規。作成済み）
- work/tasks/task-0279.md（新規。作成済み）

## やってほしいこと
1. main の作業ツリー（/home/ubuntu/ghq/github.com/tjst-t/banto）で `git status` を確認し、上記5ファイルが未コミット（変更 or 未追跡）であることを確認する
2. 上記5ファイルを**明示的に git add** する（それ以外は含めない）
3. **内容は書き換えない**（既存・作成済みの内容のまま add する）
4. 1つのコミットにまとめてよい（複数に分けてもよいが、内容は変えない）。コミットメッセージ例: "docs: 未コミットの仕事記録（task-0277/0278/0279・thread-watchdog提案書・kobo-enqueue SKILL 追記）を整理してコミット"
5. コミット後、`git status` に未コミットの変更が残っていないことを確認する
6. 検証: `npm run typecheck` と `npm test` が通ることを確認する

## 注意
- コードは変更しない。対象5ファイルの内容も書き換えない
- スコープ外のファイル（docker/**・packages/**/*.ts など）は触らない・コミットしない
- push しない（決定37）。ローカル main へ直接コミットする
- 検証コマンドは最後の1条にまとめる（全量を条件ごとに回さない）
