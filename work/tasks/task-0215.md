---
id: task-0215
type: task
kind: improvement
title: 「声が届かなかった」を「中身が悪くて落ちた」と同じ failed にしない
status: queued
written_by: kobo
environment: test
model_tier: standard
scope:
  paths: ["packages/banto-daemon/src/daemon.ts", "tests/acceptance/kobo-unreachable-is-not-failed.spec.ts"]
acceptance:
  - id: a1
    text: モジュールへ届かなかったことで終わった試行が、帳簿（イベント）を読むだけで、中身の失敗と区別できる
    verify: npm run test:one tests/acceptance/kobo-unreachable-is-not-failed.spec.ts
  - id: a2
    text: 区別の対象は「モジュールへ届かなかった」場合だけで、ツールが返したエラー（HTTP のエラー応答）は中身の失敗のままである
    verify: npm run test:one tests/acceptance/kobo-unreachable-is-not-failed.spec.ts
  - id: a3
    text: kobo.task の経緯や知らせの文面から、人が調べ直さずに「届かなかったのだから待てばよい」と判断できる
    verify: npm run test:one tests/acceptance/kobo-unreachable-is-not-failed.spec.ts
  - id: a4
    text: 届かなかったことを握り潰して成功に見せていない（I2）
    verify: npm run test:one tests/acceptance/kobo-unreachable-is-not-failed.spec.ts
  - id: a5
    text: 既存の failed の扱い（一覧・再開・集計）が壊れていない。これまで failed として数えていたものが黙って数から消えない
    verify: npm run test:one tests/acceptance/kobo-unreachable-is-not-failed.spec.ts
  - id: a6
    text: 既存の受け入れ試験が引き続き全部通る
    verify: npm test
review:
  policy: banto
---
## 背景（前提。あなたは記憶を持たないので、ここに書いてあることが全部）

### 今日の実例（これが無いと、なぜ区別が要るのか伝わらない）

2026-08-16、`banto-worker-pool.service` が OOM で繰り返し揺れた（00:30:50 / 01:26:51 / 03:06:40 / 03:51:07 / 03:54:58 / 04:06:33 / 04:35:35 の少なくとも7回）。**その最中に、Kobo の操作がモジュールへ届かず、タスクが `failed` になっている。**

| タスクが failed になった時刻 | 直前の worker-pool の OOM | 間隔 |
|---|---|---|
| 03:55:22（task-0160・報告なし） | 03:54:58 | **+24秒** |
| 04:07:20（task-0160・報告なし） | 04:06:33 | **+47秒** |

実際に `kobo.reopen` が `Failed to reach module "worker-pool" ... read ECONNRESET` で落ち、**数十秒後には同じ操作が通っている**。**中身は何も悪くない。声が届かなかっただけである。**

**今日1日で、この形の「中身は無罪なのに failed」が7回起きた**（別プロジェクト dentaku の task-0042 では、同じ理由で計5回続けて落ち、番頭が journal と帳簿を突き合わせるまで原因が分からなかった）。

### なぜ困るか

**帳簿を読む側（番頭・PO・次に見る人）から、2つが区別できない。**

- **中身が悪くて落ちた** → 直す（`kobo.reopen` の `rework`、契約の訂正、原因の調査）
- **声が届かなかった** → **待てばよい**（相手が起き直れば同じ操作が通る）

いまは**どちらも `failed`** なので、**毎回「何が悪かったのか」を人が調べ直す**ことになる。

## やること

**「相手に届かなかった」で終わった試行を、中身の失敗と区別できる形で残す。**

1. **区別の付け方は実装者が決めてよい**が、**帳簿（イベント）を読むだけで区別できること**。例：
   - 遷移理由（reason）に機械が読める印を付ける（`unreachable:<module>` など）
   - あるいは `failed` とは別の理由コードで残す
   **状態そのものを増やすかどうかは慎重に**——状態機械を膨らませると、既存の遷移の検査が全部揺れる。**まず「理由の側で区別する」形を検討し、状態を増やすなら理由を実装のコメントに残すこと。**
2. **判定は「モジュールへ届かなかった」に限る。** `packages/banto-core/src/module-invocation.ts:213` が投げる
   `Failed to reach module "<name>" at <url>: ...` がその印である。**ツール側が返したエラー**（HTTP のエラー応答）は**中身の失敗**なので、**区別の対象に含めない**。
   直す場所の当たり（実測済み）：`daemon.ts:2934` の `recordTaskFailed(..., "agent_exited_without_report")`、`daemon.ts:3233` の `audit session spawn failed: ...`、`daemon.ts:3486` の `rework session spawn failed: ...`。
3. **番頭と人に見える形にする。** `kobo.task` の経緯や、判断待ちの札・知らせの文面で、**「届かなかった」と分かること**。「なぜ落ちたか」を読む人が、**調べ直さずに『待てばよい』と判断できる**のが目的。
4. **振る舞いは変えなくてよい。** 自動で再試行する・自動で復帰させるのは別タスクの担当（今日の大掃除でいったん降ろしてある）。**このタスクは「見え方」だけを直す**——両方を1つにしないこと。

## やらないこと（スコープ外。触ったらマージ前ゲートで落ちる）

- 再試行の実装／`packages/banto-core` の変更
- OOM の側の手当て（cgroup・袋の大きさ・同時本数）
- 状態機械そのものの作り替え（状態を増やすなら**最小限**にとどめ、既存の遷移の意味を変えない）
- `packages/banto-host` / `packages/banto-worker-pool` の変更

## 気をつけること

- **「届かなかった」を握り潰して成功に見せないこと**（I2）。区別するのであって、無かったことにするのではない。
- **既存の `failed` の扱い（一覧・再開・帳簿の集計）を壊さないこと。** 区別を足しても、**いままで `failed` として数えていたものが黙って数から消えない**ようにする。

## 走らせ方（**あなた自身が OOM で死なないために**）

あなたは 2 GiB の cgroup の中にいます。**`npm test`（約400秒・2400件）や `npm run typecheck` を袋の中で回すと自分が殺されます**——今日それで職人が何本も死にました（peak 2.0 GiB ちょうど＝上限で SIGKILL）。

確かめるときは**1本だけ**：

```
npm run test:one tests/acceptance/kobo-unreachable-is-not-failed.spec.ts
```

**全量（`npm test` / `npm run typecheck`）は依頼元が袋の外から回します。**
**1本だけ通したなら「1本だけ通した」と正直に報告すること**（I1・I2）。
