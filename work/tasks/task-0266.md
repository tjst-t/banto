---
id: task-0266
type: task
kind: fix
title: 監査の失敗文言を実態に合わせる（audit_report を呼ばずに終えた）
status: queued
written_by: kobo
scope:
  paths: ["packages/banto-daemon/src/daemon.ts", "tests/acceptance/**"]
acceptance:
  - id: a1
    text: daemon.ts:3143 のエラー文言が「監査人が audit_report を一度も呼ばずに終えた」ことを表す形に直される
    verify: npm test
---
監査の失敗文言を「ツールが無い」から「監査人が audit_report を一度も呼ばずに終えた」へ直す。

# 背景
- daemon.ts:3143 のエラー文言（「このランタイムに audit_report が載っていません」）は誤解を招く。実際には roleAssignments のモデルが道具呼び出しに乗れなかっただけで、audit_report 自体は pi にも claude にも載っている
- この文言のせいで1時間ぶん誤診した（thread-167 報告）

# やり方
- daemon.ts:3143 のエラー文言を、実態に合わせて「監査人が audit_report を一度も呼ばずに終えた」と直す
- 文言のテスト（該当する acceptance テスト）も更新する

# 完了条件
上記 acceptance を満たすこと。
