---
id: task-0275
type: task
kind: improvement
title: safe-restart SKILL を4ユニット表へ更新し、デプロイゲート（system.deploy）を明記する
status: queued
written_by: kobo
refs: ["in-3a2998c9", "task-0274"]
environment: test
scope:
  paths: ["packages/banto-host/skills/**"]
acceptance:
  - id: a1
    text: safe-restart SKILL.md が4ユニット表（banto-daemon.service 含む・ExecStart 明記）へ整合し、system.restart の対象は banto.service だけ・計画的デプロイは system.deploy（ゲート付き・force のみ迂回・クラッシュ復旧は即復旧）・職人/検証/デーモンは kill -9 で systemd に拾わせる手順が記載されている
---
packages/banto-host/skills/safe-restart/SKILL.md を、task-0274（マージゲート限定＋デプロイゲート仕組化）の実装と整合する形へ更新する。task-0274 ではスコープ外（daemon/src・host/src・core/src・tests/acceptance）のため本タスクへ分離した。既に task-0274 のコミット d589e89c に変更があるので、その内容をそのまま移すか、必要なら校正して反映する。

## 期待する内容
- ユニット表を4ユニット（banto.service / banto-daemon.service / banto-worker-pool.service / banto-environment-pool.service）とし、各ユニットの ExecStart（--import tsx の起動コマンド）を明記する。
- system.restart は banto.service（番頭本体）だけを起こし直すものと明記。職人・検証環境・デーモンは別ユニットで、system.restart では反映されない。
- system.deploy（ゲート付き）が計画的デプロイの正式な口で、main に npm test を回して通ったときだけ banto.service を起こし直す。force:true で明示的にだけゲートを迂回でき、迂回は deploy-force として記録に残る。クラッシュ復旧（Restart=on-failure）はゲートを通らず即復旧。
- 職人まわり・検証環境まわり・デーモンまわりの変更反映は unit の主プロセスを kill -9 して systemd（Restart=on-failure）に拾わせる（sudo は通らない）手順を残す。

## 完了条件
- SKILL.md が上記の内容へ整合していること。既に d589e89c にある変更をそのまま移せばよい（task-0274 のコミットから SKILL.md 変更を外し、ここへ持ってくる）。
