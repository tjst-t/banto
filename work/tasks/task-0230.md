---
id: task-0230
type: task
kind: fix
title: 同時本数の上限に、監査・判定のための席を確保する（task-0223 をスコープ訂正して置き換え）
status: queued
written_by: kobo
model_tier: reasoning
scope:
  paths: ["packages/banto-worker-pool/src/pool.ts", "packages/banto-worker-pool/src/worker-tools.ts", "packages/banto-worker-pool/src/bin.ts", "packages/banto-daemon/src/daemon.ts", "tests/acceptance/worker-pool-audit-reservation.spec.ts"]
acceptance:
  - id: a1
    text: 実装の職人だけで埋めても、監査（auditor）の職人は起こせる
    verify: npm run test:one -- tests/acceptance/worker-pool-audit-reservation.spec.ts
  - id: a2
    text: 実装の職人は「上限 − 予約席」で断られ、断りにどちらの枠で断ったかと次の手が載る
    verify: npm run test:one -- tests/acceptance/worker-pool-audit-reservation.spec.ts
  - id: a3
    text: 役割を渡さない依頼は実装（executor）として数えられる
    verify: npm run test:one -- tests/acceptance/worker-pool-audit-reservation.spec.ts
  - id: a4
    text: 予約席は環境変数で変えられ、読めない値は黙って既定に落ちない（I2）
    verify: npm run test:one -- tests/acceptance/worker-pool-audit-reservation.spec.ts
  - id: a5
    text: 工房の入口（bin.ts）が予約席を読んで pool へ渡しており、稼働でも予約席が効く
    verify: npm run test:one -- tests/acceptance/worker-pool-audit-reservation.spec.ts
  - id: a6
    text: worker.list から実装と判定の内訳が読める
    verify: npm run test:one -- tests/acceptance/worker-pool-audit-reservation.spec.ts
  - id: a7
    text: task-0216 の既存の振る舞い（上限・数え方・断り方）が壊れていない
    verify: npm run test:one -- tests/acceptance/worker-pool-concurrency-limit.spec.ts
  - id: a8
    text: 全量と型検査が通る
    verify: npm test && npm run typecheck
---
## これは task-0223 の積み直し（契約のスコープだけを訂正したもの）

**中身は task-0223 で完成しています。**マージ前ゲートが `packages/banto-worker-pool/src/bin.ts`
（3行）をスコープ違反で止め、**PO が「bin.ts を足して通す」と裁定しました**（取次 in-491f752a・
2026-08-16）。`kobo.amend` は緩める方向の改訂を番頭に許さないため、決定64 のとおり**新しく積んで
置き換えます**。**やり直しではありません。**

### まずやること：既にある成果を土台にする

ブランチ **`task/task-0223`** に2本のコミットがあります。**ゼロから書き直さないこと。**

- `d8617240` — スコープ内の実装（pool.ts / worker-tools.ts / daemon.ts / 新 spec）
- `b6385eb1` — **今回スコープに入った** bin.ts の3行（入口で `BANTO_WORKER_AUDIT_RESERVED` を
  読んで pool へ渡す）

`git log -p task/task-0223` で中身を読み、**2本とも自分の作業ブランチへ取り込んで**ください
（cherry-pick で構いません）。取り込んだうえで、下の「やること」と受け入れ条件を満たしているかを
自分で確かめ、足りなければ直します。

## やること（task-0223 の契約そのまま）

1. **役割を工房まで運ぶ。** `worker.delegate_toolkit` の入力に `role`（`executor` / `auditor`）を足し、
   `DelegateInput` から `reserveSlot` まで通す。**省略時は `executor`**——番頭が直に起こす職人も
   実装側として数える。Kobo は `delegateWorker` が持っている役割をそのまま渡す。
2. **判定側に席を確保する。** 実装（`executor`）は **上限 − 予約席** まで、監査・判定（`auditor`）は
   **上限** まで。予約席は環境変数で変えられ（`BANTO_WORKER_AUDIT_RESERVED`、既定2）、読めない値は
   黙って既定に落とさない（I2）。断りの文面には**どちらの枠で断ったか**と次の手を書く。合印
   `BANTO_WORKER_LIMIT` はそのまま。
3. **入口で読む（今回スコープに入った分）。** `packages/banto-worker-pool/src/bin.ts` で
   `BANTO_WORKER_AUDIT_RESERVED` を読んで pool へ渡す（`BANTO_WORKER_MAX_CONCURRENT` と同じ流儀）。
   **これが無いと予約席は既定0で立ち、稼働では1ミリも効かない。** ヘッダの環境変数一覧にも追記する。
4. **覗き窓に出す。** `worker.list` に実装／監査の内訳を出す（`details.concurrency` にも構造で）。
5. **既定6の内訳を書き直す。** 「Kobo の5本＋番頭の1本」→「実装4＋監査・判定2」。
   **6 そのものは上げない。**

## やらないこと

- 待ち行列は作らない（task-0216・D3）。満杯なら断る。
- 満杯を `failed` にしない件は task-0222 で着地済み。断り方（合印）はそれと揃える。
- task-0216 の試験（`worker-pool-concurrency-limit.spec.ts`）は**スコープ外**。壊さずに通ること
  （予約席は渡されたときだけ効く、という task-0223 の判断がその答え）。

## 走らせ方（あなた自身が殺されないために）

**袋の中で `npm test` と `npm run typecheck` を回さないこと。** 職人1本には 2 GiB の袋（cgroup）が
貼られていて、全量（約2400件・400秒）を回すと当たって**あなたごと kill されます**（今日それで
task-0222 の職人が死にました。peak 1.7GiB）。

- 回すのは **`npm run test:one -- tests/acceptance/worker-pool-audit-reservation.spec.ts`** と
  **`npm run test:one -- tests/acceptance/worker-pool-concurrency-limit.spec.ts`** の2本だけ。
- `npm test && npm run typecheck` は**依頼元（番頭）とゲートが袋の外で回す**ので、あなたは回さない。
- `packages/banto-daemon/src/daemon.ts` は 223KB の一枚岩なので**全文を読まない**（grep で当てる）。
- **回した本数は報告に正直に書くこと。**
