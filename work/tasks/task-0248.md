---
id: task-0248
type: task
kind: feat
title: ランタイム別想定消費リソースの定義と環境変数解決
status: done
scope:
  paths: ["packages/banto-worker-pool/src/backends.ts", "packages/banto-worker-pool/src/bin.ts", "packages/banto-worker-pool/src/pool.ts", "tests/acceptance/**"]
acceptance:
  - { id: a1, text: "RuntimeRegistration に assumedResources（memoryMiB）を持つ", verify: "npm test" }
  - { id: a2, text: "BANTO_WORKER_PI_MEMORY_MB（既定300）と BANTO_WORKER_CLAUDE_MEMORY_MB（既定1200）を bin.ts で解決し、WorkerPoolOptions に反映する", verify: "npm test" }
  - { id: a3, text: "環境変数で想定値を変えたとき、pool の設定に反映される（テストで確認）", verify: "npm test" }
---

# 依頼

Worker Pool の並行制御を「想定消費リソース」ベースに変える設計（work/reports/2026-08-17-worker-resource-based-concurrency.md・accepted）のタスク B。

`RuntimeRegistration` に `assumedResources: ResourceEstimate`（memoryMiB）を追加し、bin.ts で環境変数を解決して WorkerPool へ渡す。

- `BANTO_WORKER_PI_MEMORY_MB`（既定 300 MiB）— pi-rpc ランタイム
- `BANTO_WORKER_CLAUDE_MEMORY_MB`（既定 1200 MiB）— claude-agent-sdk ランタイム
- （第2段）`BANTO_WORKER_PI_CPU_FRACTION` / `BANTO_WORKER_CLAUDE_CPU_FRACTION` は今回は未実装でよい

設計書の「1. 想定消費リソースの定義」を参照。

## 完了条件
上記 acceptance をすべて満たすこと。タスク A（task-0247）の host-resources.ts が前提。
