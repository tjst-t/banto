---
id: task-0092
type: task
kind: fix
title: npm test が負荷で間欠的に落ちる問題の調査と修正
status: queued
scope:
  paths: ["tests/**", "package.json"]
acceptance:
  - { id: a1, text: "間欠失敗の原因（負荷がどのようにテストへ影響するか）が特定され、報告にまとめられる", verify: "npm test" }
  - { id: a2, text: "修正後、npm test を複数回（職人が3回以上）実行して落ちないことを確認する", verify: "npm test" }
---

# 依頼

## 何が起きたか（2026-08-11・task-0089 の検証中に発見）

task-0089（Environment Pool の provision 順序）の検証中、`npm test` が**負荷で間欠的に落ちる**ことが分かった。

計測した範囲（task-0089 の職人による）:
- 本ブランチの全体実行 5回のうち 3回クリーン、1回 2件失敗、1回 1件失敗
- 落ちたのは `cli-events.spec.ts`（CLI サブプロセスの1行を 3 秒待つだけの試験）と `env-driver-contract.spec.ts` の `list`
- base（stash 状態）でも `env-*.spec.ts` だけを3回回すと 2回 docker 系がまとめて落ちた
- **task-0089 の変更が原因ではない**（触っていない箇所が落ちる）が、**機構が壊れている合図**（P6）
- 本試験の追加で docker 環境が3つ増えており、負荷は増している

## 調査してほしいこと

1. 間欠失敗の原因を特定する:
   - 負荷の要因（docker 環境の数・並列実行・リソース・タイムアウト）
   - `cli-events.spec.ts` の「1行を3秒待つ」がなぜ負荷で落ちるか（サブプロセスの起動遅延・タイムアウト不足）
   - `env-driver-contract.spec.ts` の `list` がなぜ落ちるか（docker の応答遅延・並列実行の干渉）
2. 修正の方針を決める（テストの待ち時間・タイムアウトの調整・並列度の制御・テストの分離・リソースの確保など）
3. 実装して、npm test を複数回実行して安定することを確かめる

## 完了条件

上記 acceptance をすべて満たすこと。a1 は「原因が特定され報告にまとまる」こと（修正が入らなくても、原因と対処方針が明確になれば良い）。a2 は「複数回実行して落ちない」こと。

## 注意

- スコープは `tests/**` と `package.json`（テストコードとテスト実行設定）。パッケージのコード（例: タイムアウトの定数）を直す必要が出たら、その旨を報告してスコープ拡張を相談すること
- 間欠失敗は再現が安定しないことがある。計測（何回実行して何回落ちたか）を記録して報告すること（I1: 通ったつもりで出さない）
