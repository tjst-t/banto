---
id: task-0268
type: task
kind: feature
title: 監査ロールに道具呼び出し未実証のモデルを当てられないようにする（再起票）
status: queued
written_by: kobo
refs: ["task-0265", "in-0a2c73b6"]
scope:
  paths: ["packages/banto-daemon/src/**", "packages/banto-web/src/**", "tests/acceptance/**"]
acceptance:
  - id: a1
    text: roleAssignments で監査ロール（auditor）に当てるモデルが、audit_report 等の道具呼び出しに対応しているかを検証し、未実証のモデルを当てようとすると起こす前に弾く・警告する
    verify: npm test
review:
  policy: auto
---
監査ロール（auditor）に、道具呼び出し（audit_report 等）が実証されていないモデルを当てられないようにする（再発防止）。task-0265 の実装（ブランチ task/task-0265、コミット 8001a34e）は監査2回 pass 済みで正しい。スコープに tests/acceptance/** を追加（PO 判断 in-0a2c73b6）。

## やること
- ブランチ task/task-0265 の実装（8001a34e）を再利用すること。git branch -a または git log --all で探し、その内容を確認する。無ければ main から同等の実装を行う（設計は work/tasks/task-0265.md を参照）
- 実装内容：
  1. kobo-settings.ts: RoleAssignmentStore に任意メソッド toolCallProvenModelNames?() を追加（監査の口 audit_report の道具呼び出しを実証済みのモデル名。空・未定義＝確かめられないときは通す）。createKoboSettings に第3引数 options.allowUnprovenAuditModel を追加。write() で監査の役（next.audit.model）に名指しされたモデルが実証済み一覧に無ければ、職人を起こす前にエラーで弾く。当てたい場合は allowUnprovenAuditModel で明示的に許可できる
  2. daemon.ts: toolCallProvenModelNames() を追加（実証済み白リスト = Claude Code 別名 opus/sonnet/haiku、標準 worker の opencode-go/deepseek-v4-flash。huihui/deepseek-v4-flash-abliterated は判定の口に乗れなかった実績があるため載せない）。delegateWorker で監査の役に未実証モデルを当てたままなら起こす前に警告ログ
  3. http-server.ts: createKoboSettings の配線に toolCallProvenModelNames を追加
  4. tests/acceptance/kobo-worker-integration.spec.ts: 未実証モデルを監査の役に当てると保存の時点で断る／実証済みは通る／allowUnprovenAuditModel で許可できる／一覧が空なら通す、という受け入れテストを追加（[kobo-roles] 群）
- main を取り込んでから再提出すること（task-0265 はマージ前ゲートで tests/acceptance/** がスコープ外扱いで落ちた。今回はスコープに含まれているので、acceptance テストも含めて提出してよい）
- 検証: npm ci 後に npm run test:one tests/acceptance/kobo-worker-integration.spec.ts と npm test / typecheck / build を確認
