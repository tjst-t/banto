---
id: task-0098
type: task
kind: fix
title: gate-review-fixes fix2-b のコンテナ決定的失敗を調査・修正
status: queued
scope:
  paths: ["tests/acceptance/gate-review-fixes.spec.ts", "packages/banto-daemon/src/**", "packages/banto-repo-manager/src/**"]
acceptance:
  - { id: a1, text: "fix2-b がコンテナ内で決定的に失敗する原因が特定され、報告にまとめられる", verify: "調査レポートまたは会話での説明" }
  - { id: a2, text: "修正後、ホストでもコンテナ内でも fix2-b が通る", verify: "npm test -- tests/acceptance/gate-review-fixes.spec.ts" }
  - { id: a3, text: "npm test（docker 系7本除外後）がコンテナ内でも通る", verify: "docker compose -f docker/test.yaml run --rm -v <repo>:<repo>:ro test npm test" }
---

# 依頼

## 何が起きたか

task-0093 の調査で、`tests/acceptance/gate-review-fixes.spec.ts` の `fix2-b` が
**コンテナ内でのみ決定的に失敗する**ことが判明した。

- ホスト単体実行: 3/3 pass
- コンテナ内（Node 22）: 5/5 fail
- コンテナ内（Node 24）: 3/3 fail

症状: 「期待2に対し実際3」——`gate_evaluated` の重複排除が効いていない様子。

## やること

1. `fix2-b` がコンテナ内で決定的に失敗する原因を特定する
   - コンテナとホストの違い（ファイルシステム、タイムゾーン、git state、環境変数など）
   - `gate_evaluated` イベントの発行・重複排除ロジック
2. 原因を修正する
3. ホスト・コンテナ双方で fix2-b が通ることを確認する

## 注意

- スコープは `tests/acceptance/gate-review-fixes.spec.ts` と、必要に応じて `packages/banto-daemon/src/**` または `packages/banto-repo-manager/src/**`
- 修正が他のパッケージに広がる場合は、スコープ拡張を相談すること
- 間欠失敗ではなく決定的失敗なので、再現は安定しているはず
