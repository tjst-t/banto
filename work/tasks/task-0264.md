---
id: task-0264
type: task
kind: fix
title: 検証前にワークツリーの node_modules シンボリックリンクを外す（再起票）
status: queued
written_by: kobo
scope:
  paths: ["packages/banto-environment-pool/src/docker-driver.ts", "tests/acceptance/env-worktree-node-modules-symlink.spec.ts"]
acceptance:
  - id: a1
    text: ワークツリー直下の node_modules がシンボリックリンクなら、compose を読む前に外す（リンクだけを lstat で見分けて unlink）
    verify: npm test
  - id: a2
    text: リンク先の実体・本物のディレクトリには一切触らない（シンボリックリンクでなければ即 return）
    verify: npm test
  - id: a3
    text: 外したときは理由（コンテナの中からはホストの絶対パスを指せず @banto/* が解決できなくなる）付きで stderr にログを残す。symlink が無ければ誤報しない
    verify: npm test
  - id: a4
    text: symlink を外したあとも setup（compose run --rm）が呼ばれる（cache/primed のロジックとは独立）
    verify: npm test
  - id: a5
    text: npm test と npm run typecheck が通る
    verify: npm test && npm run typecheck
---
検証を立てる前に、ワークツリーに残った node_modules のシンボリックリンクを外す（task-0240 の再起票）。

# 背景
- task-0240 の実装は**完了済み**（task/task-0240 ブランチ・コミット済み: docker-driver.ts の handleProvision に stripWorktreeNodeModulesSymlink() を追加、tests/acceptance/env-worktree-node-modules-symlink.spec.ts 6ケース）
- task-0240 は paused のまま職人が消失し再開できなかったため abandon した。**実装を引き継いで、検証・監査を回す**ことが本タスクの目的
- 前提: bin.ts(443) の `` `where` `` エスケープ欠落（typecheck を壊していた）は `2b234322` で main に着地済み

# やり方
- 実装は task/task-0240 ブランチから引き継ぐ（cherry-pick または内容を確認して同様に実装）
- 新規試験 tests/acceptance/env-worktree-node-modules-symlink.spec.ts（6ケース: a1 symlink が外れる・a2 リンク先の実体は残る・a3 本物のディレクトリは触らない・a4 外した理由が stderr に残る／symlink が無ければ誤報しない・a5 外したあとも setup が呼ばれる・a6 型検査）
- 検証は npm test と npm run typecheck（Kobo の検証環境で回る）

# 完了条件
上記 acceptance をすべて満たすこと。
